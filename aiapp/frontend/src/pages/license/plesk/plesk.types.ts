// ============================================================
// PLESK TYPES - Types spécifiques Plesk
// ============================================================

export interface PleskLicense {
  id: string;
  ip: string;
  version: string;
  domainNumber: number;
  status: string;
  createdAt: string;
}
