  src/pages/{NAV1}/{NAV2}/
  │
  ├── {nav2}.types.ts          # Types partagés (SEUL partage JS autorisé)
  ├── styles.css               # Layout uniquement
  ├── index.tsx                # Routing
  │
  ├── {groupe}/                # ══════ GROUPE ══════
  │   ├── index.ts             # Exports modales partagées du groupe
  │   ├── {Modal}Modal.tsx     # Modales partagées DU GROUPE
  │   │
  │   └── {nav3}/              # ══════ TAB ISOLÉ ══════
  │       ├── index.ts         # Barrel export
  │       ├── {Nav3}Tab.tsx    # Composant principal
  │       ├── {Nav3}Tab.service.ts  # Service API isolé
  │       └── {Nav3}Tab.css    # Styles isolés (.{nav3}-*)

il faut ajouter que 
- le fichier services.ts pour les calls d'api on le met pour chaque NAV4 directement dans le repertoire
- on ne cree pas le repertoire AB/Modals on migre AB/Modals/* vers AB/Modals
- on ne cree pas les repertoire AB/tabs/XY si ça existe, on deplace vers AB/XY et on reecrit tous les imports
