================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.list.svg
Route:   /web-cloud/domain
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                              |
|-----------------------|-----------------------------------------|
| example.com           | /domain → item                          |
| .com                  | /domain/{d} → extension                 |
| Actif                 | /domain/{d} → state                     |
| 15/03/2026            | /domain/{d} → expiration                |
| Automatique           | /domain/{d}/serviceInfos → renew        |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Domaines et DNS actif
3. MAIN CONTENT (Full width - pas de left panel)
   - Titre: Mes noms de domaine
   - Actions bar:
     - Bouton "Commander un domaine"
     - Bouton "Transférer"
     - Bouton "Exporter CSV"
     - Champ recherche
   - Table domaines
     - Colonnes: Checkbox, Nom, Extension, État, Expiration, Renouvellement, Actions
     - Badges états: Actif (vert), Expire bientôt (jaune), Expiré (rouge)
     - Context menu: Gérer, Zone DNS, Renouveler, Transférer
   - Pagination

# ÉTATS DOMAINE
| Badge | Couleur | Condition |
|-------|---------|-----------|
| Actif | #DEF7EC (vert) | state = "ok" |
| Expire bientôt | #FEF3C7 (jaune) | expiration < 30 jours |
| Expiré | #FEE2E2 (rouge) | state = "expired" |

# API ENDPOINTS
GET /domain - Liste tous les domaines
GET /domain/{domain} - Détails domaine
POST /order/cart/{cartId}/domain - Commander
POST /domain/{domain}/renew - Renouveler

================================================================================
