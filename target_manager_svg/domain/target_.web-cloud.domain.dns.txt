================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.dns.svg
Route:   /web-cloud/domain/{domainName}/zone (variante style)
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                              |
|-----------------------|-----------------------------------------|
| example.com.          | /domain/zone/{z}/record → subDomain     |
| A, AAAA, CNAME...     | /domain/zone/{z}/record → fieldType     |
| 3600, 86400           | /domain/zone/{z}/record → ttl           |
| 203.0.113.50          | /domain/zone/{z}/record → target        |

================================================================================

# STRUCTURE PAGE
(Variante de dns-zone avec style NAV3 texte souligné)

1. NAV1 - Header OVH
2. NAV2 - Domaines et DNS actif
3. LEFT PANEL - Liste domaines
4. RIGHT PANEL
   - Titre: Zone DNS de {domainName}
   - NAV3 style texte souligné (pas boutons)
   - Toolbar: Ajouter, Importer, Exporter, Réinitialiser, Recherche
   - Table records DNS
     - Colonnes: Domaine, Type, TTL, Cible, Actions
     - Types colorés par texte (pas badges)

# API ENDPOINTS
GET /domain/zone/{zone}/record - Liste records
POST /domain/zone/{zone}/record - Créer
PUT /domain/zone/{zone}/record/{id} - Modifier
DELETE /domain/zone/{zone}/record/{id} - Supprimer

================================================================================
