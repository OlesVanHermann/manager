# Cahier des Charges : bare-metal/ip - Page Firewall Game DDoS

## 1. VUE D'ENSEMBLE
- **Type** : Page de configuration avec tableau éditable
- **Contexte** : Protection DDoS pour serveurs de jeux
- **Objectif** : Configurer les règles du Game Firewall pour protéger les serveurs de jeux

## 2. DIMENSIONS
| Propriété | Valeur |
|-----------|--------|
| Largeur | 1100px |
| Hauteur | 550px |

## 3. COMPOSANTS UI
- **OdsText** : Titres, descriptions
- **OdsLink** : Retour, guides
- **OdsButton** : Ajouter règle
- **OdsToggle** : Politique par défaut
- **OdsDatagrid** : Tableau des règles
- **OdsSelect** : Protocole (inline)
- **OdsInput** : Ports (inline)
- **OdsBadge** : État
- **OdsPagination** : Navigation

## 4. HEADER
| Propriété | Valeur |
|-----------|--------|
| Lien retour | "← Retour aux IPs" (bleu) |
| Titre | "Firewall Game DDoS" |
| Bouton guides | "Guides ▼" (secondaire) |

## 5. DESCRIPTION
| Ligne | Texte |
|-------|-------|
| 1 | "Le firewall Game DDoS protège vos serveurs de jeux contre les attaques." |
| 2 | "Configurez les ports autorisés pour vos protocoles de jeu." (gris) |

## 6. BARRE D'ACTIONS

### Bouton Ajouter
| Propriété | Valeur |
|-----------|--------|
| Texte | "+ Ajouter une règle" |
| Style | Primary (bleu) |

### Toggle Politique
| Propriété | Valeur |
|-----------|--------|
| Label | "Appliquer la politique par défaut" |
| Type | OdsToggle |
| État | Activé (bleu) |

## 7. TABLEAU DES RÈGLES

### Colonnes
| Colonne | Type | Description |
|---------|------|-------------|
| Protocole | Texte | UDP, TCP, UDP + TCP |
| Port début | Texte | Numéro de port |
| Port fin | Texte | Numéro de port |
| État | Badge | OK, En cours |
| Actions | Icône | 🗑 (supprimer) |

### Badge État OK
| Propriété | Valeur |
|-----------|--------|
| Fond | #DCFCE7 |
| Texte | #166534 |

### Badge État En cours
| Propriété | Valeur |
|-----------|--------|
| Fond | #DBEAFE |
| Texte | #1E40AF |

### Ligne d'ajout (inline)
| Propriété | Valeur |
|-----------|--------|
| Fond | Vert clair (#F0FDF4) |
| Bordure | Vert (#10B981), pointillée |
| Champs | Select protocole, Input ports |
| Actions | ✕ (rouge), ✓ (vert) |

## 8. PAGINATION
| Propriété | Valeur |
|-----------|--------|
| Format | "{count} règles sur 10 maximum" |
| Navigation | ‹ [1] › |

## 9. APIS & ENDPOINTS
- GET /ip/{ip}/game/rule - Liste des règles
- POST /ip/{ip}/game/rule - Ajouter une règle
- DELETE /ip/{ip}/game/rule/{id} - Supprimer une règle
- PUT /ip/{ip}/game - Activer/désactiver politique

