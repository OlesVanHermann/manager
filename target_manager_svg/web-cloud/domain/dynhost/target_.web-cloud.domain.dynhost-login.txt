================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.dynhost-login.svg
Route:   /web-cloud/domaines/domain/{domainName}/dynhost/logins
Version: v1 (2025-12-30)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                     | Source API                              |
|---------------------------------|-----------------------------------------|
| example.com-home                | GET /domain/zone/{z}/dynHost/login → login |
| home.example.com                | GET /domain/zone/{z}/dynHost/login → subDomain |
| Actif                           | GET /domain/zone/{z}/dynHost/login → status |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Domaines et DNS actif
3. LEFT PANEL - Liste domaines (280px)
4. RIGHT PANEL (1096px)
   - Header: Accès DynHost
   - NAV3: 10 tabs, DynHost actif
   - NAV4: Entrées DynHost | Accès/Logins (actif)
   - TOOLBAR: + Créer un login
   - TABLE: Identifiant, Sous-domaine associé, Statut, Actions
   - PAGINATION
   - HELP LINK

# ACTIONS
- + Créer un login → modal-dynhost-login-add
- Changer MDP → modal-dynhost-login-password
- ⋮ → Modifier / Supprimer

# API ENDPOINTS
GET /domain/zone/{zoneName}/dynHost/login
GET /domain/zone/{zoneName}/dynHost/login/{login}
POST /domain/zone/{zoneName}/dynHost/login
PUT /domain/zone/{zoneName}/dynHost/login/{login}
POST /domain/zone/{zoneName}/dynHost/login/{login}/changePassword
DELETE /domain/zone/{zoneName}/dynHost/login/{login}

================================================================================
