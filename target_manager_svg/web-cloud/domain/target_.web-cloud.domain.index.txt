================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.index.svg
Route:   /web-cloud/domain (liste des domaines)
Version: v1 (2025-12-29)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                              |
|--------------------------|-----------------------------------------|
| example.com              | GET /domain → serviceName               |
| Actif                    | GET /domain/{dn} → status               |
| 15/03/2026               | GET /domain/{dn} → expirationDate       |
| OVH                      | GET /domain/{dn}/nameServer → type      |
| DNSSEC                   | GET /domain/{dn}/dnssec → enabled       |
================================================================================

# STRUCTURE PAGE
1. HEADER - Titre "Noms de domaine"
2. TOOLBAR - Recherche + Bouton "Commander un domaine"
3. TABLE - Liste domaines avec colonnes:
   - Domaine (lien vers détail)
   - Statut (badge: Actif/Expire bientôt/Expiré)
   - Expiration (date)
   - DNS (OVH/Externe)
   - Protection (DNSSEC badge)
   - Actions (menu contextuel)
4. PAGINATION

# API ENDPOINTS
GET /domain - Liste des domaines du compte
GET /domain/{domainName} - Détail domaine
GET /domain/{domainName}/nameServer - Serveurs DNS
GET /domain/{domainName}/dnssec - Statut DNSSEC
================================================================================
