================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.carrier-sip.general.svg
Route:   /web-cloud/voip/carrier-sip/{trunkId}
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| trunk-ab12345-1           | GET /telephony/{ba}/carrierSip → item   |
| Production                | GET /telephony/{ba}/carrierSip/{id} → description |
| 12                        | GET /telephony/{ba}/carrierSip/{id}/cdrs → currentCalls |
| 50 canaux max             | GET /telephony/{ba}/carrierSip/{id} → maxChannels |
| 847                       | GET /telephony/{ba}/carrierSip/{id}/cdrs → todayCalls |
| 3:42                      | GET /telephony/{ba}/carrierSip/{id}/statistics → avgDuration |
| 98.2%                     | GET /telephony/{ba}/carrierSip/{id}/statistics → successRate |
| sip.ovh.net:5060          | GET /telephony/{ba}/carrierSip/{id} → sipServer |
| 192.168.1.0/24            | GET /telephony/{ba}/carrierSip/{id}/acl → list |
| 15 847 minutes            | GET /telephony/{ba}/carrierSip/{id}/consumption → minutes |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk] - Trunk actif
   - Recherche
   - Liste des trunks SIP
4. RIGHT PANEL
   - Header: ID trunk + description + badge status
   - NAV4: [Général][Paramètres][Routage][CDR]
   - Stats cards (4):
     * Appels en cours / max canaux
     * Appels aujourd'hui
     * Durée moyenne
     * Taux de succès
   - Section Informations de connexion:
     * Serveurs SIP (Primary/Backup)
     * Identifiant + mot de passe
     * Protocole/Codec/DTMF
   - Section Configuration réseau:
     * IP autorisées (ACL)
     * Actions: Gérer IP, Tester connexion
   - Section Détails du service:
     * Offre, canaux, numéros associés
     * Dates création/renouvellement
     * Consommation + forfait

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic trunk → Charge les détails du trunk
- Clic NAV4 → Navigation vers l'onglet
- Clic 📋 → Copier valeur
- Clic 👁 → Afficher/masquer mot de passe
- Clic Gérer les IP → Modal gestion ACL
- Clic Tester la connexion → Test SIP
- Clic Voir les factures → Navigation facturation
- Clic Modifier l'offre → Modal changement offre

================================================================================

API ENDPOINTS

GET /telephony/{ba}/carrierSip → Liste trunks
GET /telephony/{ba}/carrierSip/{id} → Détails trunk
GET /telephony/{ba}/carrierSip/{id}/cdrs → Statistiques appels
GET /telephony/{ba}/carrierSip/{id}/acl → Liste ACL
POST /telephony/{ba}/carrierSip/{id}/acl → Ajouter IP
DELETE /telephony/{ba}/carrierSip/{id}/acl/{ip} → Supprimer IP
GET /telephony/{ba}/carrierSip/{id}/consumption → Consommation
GET /telephony/{ba}/carrierSip/{id}/statistics → Statistiques

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
