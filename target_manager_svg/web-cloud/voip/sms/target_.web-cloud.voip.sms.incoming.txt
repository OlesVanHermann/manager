================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.sms.incoming.svg
Route:   /web-cloud/voip/{billingAccount}/sms/{serviceName}/incoming
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| sms-abc123            | Route param {serviceName}                     |
| +33 6 00 00 00 00     | GET /sms/{sn}/receivers → virtualNumber       |
| https://api.example.. | receivers → webhookUrl                        |
| 127                   | GET /sms/{sn}/incoming → count                |
| 30/12 15:42           | incoming[].receivedAt                         |
| +33 6 12 34 56 78     | incoming[].sender                             |
| STOP - Je souhaite... | incoming[].message                            |
| Délivré               | incoming[].webhookStatus                      |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL - Liste des comptes SMS
4. RIGHT PANEL
   - Header: Nom compte + breadcrumb /SMS entrants
   - NAV3: Général | Envoyer | Campagnes | [Entrants] | Historique | Modèles
   - Card "Configuration réception": Numéro, Webhook URL
   - Toolbar: Refresh, Recherche, Filtre période, Export CSV
   - Cards stats: SMS reçus, Expéditeurs uniques, Webhooks délivrés
   - Table SMS entrants: Date, Expéditeur, Message, Webhook, Actions
   - Pagination

# COLONNES TABLE
| Colonne | Largeur | Contenu |
|---------|---------|---------|
| Date/Heure | 130px | Format DD/MM HH:MM |
| Expéditeur | 150px | Numéro téléphone |
| Message | 450px | Contenu SMS (tronqué) |
| Webhook | 150px | Badge statut délivrance |
| Actions | 156px | Voir / Supprimer |

# INTERACTIONS
- Clic NAV3 tab → Navigation onglet
- Clic ↻ → Rafraîchir liste
- Clic "Configurer la réception" → Modal/Page configuration webhook
- Dropdown période → Filtrer par date
- Clic 👁 → Modal détail SMS complet
- Clic ✕ → Supprimer SMS (confirmation)
- Clic "Exporter CSV" → Téléchargement fichier

# API ENDPOINTS
GET /sms/{sn}/receivers                    → Configuration réception
PUT /sms/{sn}/receivers                    → Modifier configuration
GET /sms/{sn}/incoming                     → Liste SMS entrants
GET /sms/{sn}/incoming/{id}                → Détail SMS
DELETE /sms/{sn}/incoming/{id}             → Supprimer SMS
GET /sms/{sn}/incoming/export              → Export CSV
GET /sms/{sn}/incoming/statistics          → Stats réception

================================================================================
