================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.carrier-sip.settings.svg
Route:   /web-cloud/voip/carrier-sip/{trunkId}/settings
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| trunk-ab12345-1           | GET /telephony/{ba}/carrierSip → item   |
| Production                | GET /telephony/{ba}/carrierSip/{id} → description |
| UDP                       | GET /telephony/{ba}/carrierSip/{id} → protocol |
| 5060                      | GET /telephony/{ba}/carrierSip/{id} → port |
| G.711a, G.711u, G.729     | GET /telephony/{ba}/carrierSip/{id} → codecs |
| RFC2833                   | GET /telephony/{ba}/carrierSip/{id} → dtmfMode |
| 1800 secondes             | GET /telephony/{ba}/carrierSip/{id} → sessionTimer |
| Activé/Désactivé          | GET /telephony/{ba}/carrierSip/{id} → options |
| 50                        | GET /telephony/{ba}/carrierSip/{id} → maxChannels |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk] - Trunk actif
   - Recherche
   - Liste des trunks SIP
4. RIGHT PANEL
   - Header: ID trunk + description + badge status
   - NAV4: [Général][Paramètres][Routage][CDR]
   - Titre: Paramètres du trunk
   - Section Configuration SIP:
     * Description
     * Protocole transport (dropdown)
     * Port SIP
     * Codecs audio (configurable)
     * Mode DTMF (dropdown)
     * Session Timer
   - Section Options avancées:
     * Toggles: REGISTER, Auth digest, NAT, Caller ID, TLS
   - Section Sécurité:
     * Mot de passe SIP (masqué)
     * Actions: Modifier, Régénérer
   - Section Limites d'appels:
     * Max simultanés
     * Appels/minute
     * Durée max

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic trunk → Charge les détails du trunk
- Clic NAV4 → Navigation vers l'onglet
- Clic dropdown → Sélection option
- Toggle ON/OFF → Active/désactive option
- Clic 👁 → Afficher/masquer mot de passe
- Clic Modifier mot de passe → Modal édition
- Clic Régénérer aléatoire → Génération nouveau mdp
- Clic Configurer codecs → Modal ordre codecs
- Clic Enregistrer → Sauvegarde paramètres

================================================================================

API ENDPOINTS

GET /telephony/{ba}/carrierSip → Liste trunks
GET /telephony/{ba}/carrierSip/{id} → Détails trunk
PUT /telephony/{ba}/carrierSip/{id} → Modifier paramètres
PUT /telephony/{ba}/carrierSip/{id}/settings → Modifier config SIP
PUT /telephony/{ba}/carrierSip/{id}/password → Modifier mot de passe
POST /telephony/{ba}/carrierSip/{id}/password/generate → Régénérer

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
