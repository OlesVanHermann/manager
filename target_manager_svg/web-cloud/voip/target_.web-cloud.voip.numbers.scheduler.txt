================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.number.scheduler.svg
Route:   /web-cloud/voip/{billingAccount}/number/{serviceName}/scheduler
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| +33 800 123 456       | Route param {serviceName}                     |
| Ouvert                | Calcul basé sur schedule actuel               |
| 09:00 - 12:00         | GET scheduler → slots[].timeFrom/timeTo       |
| Fermé                 | Jours sans slots configurés                   |
| 3 jours fériés        | GET scheduler/holidays → count                |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL - Liste des numéros
4. RIGHT PANEL
   - Header: Numéro + badge type + breadcrumb /Horaires
   - NAV3: Général | Configuration | Agents | Statistiques | [Horaires]
   - Bannière statut (ouvert/fermé + prochaine transition)
   - Grille horaires semaine (visualisation + édition)
   - Section jours fériés et exceptions

# REPRÉSENTATION VISUELLE
| Couleur | Signification |
|---------|---------------|
| Vert DEF7EC | Plage ouverte |
| Rouge FEE2E2 | Plage fermée |
| Orange FEF3C7 | Effectif réduit |

# INTERACTIONS
- Clic NAV3 tab → Navigation onglet
- Clic ✎ jour → Modal édition horaires du jour
- Clic "Gérer exceptions" → Page exceptions/fériés
- Drag sur plage horaire → Ajuster les horaires (optionnel)

# API ENDPOINTS
GET /telephony/{ba}/number/{sn}/easyHunting/scheduler → Config horaires
PUT /telephony/{ba}/number/{sn}/easyHunting/scheduler → Modifier horaires
GET /telephony/{ba}/number/{sn}/easyHunting/scheduler/holidays → Jours fériés
POST /telephony/{ba}/number/{sn}/easyHunting/scheduler/holidays → Ajouter férié

# STRUCTURE SCHEDULER
```json
{
  "monday": [
    {"timeFrom": "09:00", "timeTo": "12:00"},
    {"timeFrom": "14:00", "timeTo": "18:00"}
  ],
  "saturday": [],
  "sunday": []
}
```

================================================================================
