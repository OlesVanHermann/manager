================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.line.softphone.svg
Route:   /web-cloud/voip/line/{serviceNumber}/softphone
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| +33 1 23 45 67 89         | GET /telephony/{ba}/line → item         |
| Poste Accueil             | GET /telephony/{ba}/line/{sn} → description |
| sip.ovh.fr                | GET /telephony/{ba}/line/{sn}/phone → sipServer |
| 0033123456789             | GET /telephony/{ba}/line/{sn}/phone → sipUsername |
| ••••••••••••              | GET /telephony/{ba}/line/{sn}/phone → sipPassword |
| iPhone 14 Pro             | GET /telephony/{ba}/line/{sn}/softphone/devices → name |
| Connecté                  | GET /telephony/{ba}/line/{sn}/softphone/devices → status |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk]
   - Recherche
   - Liste des lignes
4. RIGHT PANEL
   - Header: Numéro + description + badge status
   - NAV4: [Général][Consommation][Appels][Téléphone][Options]
   - Sous-tabs: Matériel, Softphone (actif), Touches
   - Titre: Configuration Softphone
   - Bannière info application avec lien téléchargement
   - Card identifiants de connexion:
     * Serveur SIP (copiable)
     * Nom d'utilisateur (copiable)
     * Mot de passe SIP (masqué, copiable, régénérable)
   - Card QR code:
     * QR code pour configuration automatique
     * Instructions pas à pas
     * Bouton régénérer QR
   - Section appareils connectés:
     * Liste des appareils avec statut
     * Bouton ajouter appareil

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic ligne → Charge les détails de la ligne
- Clic NAV4 → Navigation vers l'onglet
- Clic sous-tab → Navigation vers la fonctionnalité
- Clic 📋 → Copie dans le presse-papier
- Clic 👁 → Affiche/masque le mot de passe
- Clic Régénérer → Nouveau mot de passe SIP
- Clic ↗ Télécharger → Lien vers stores
- Clic ↻ Régénérer QR → Nouveau QR code
- Clic + Nouvel appareil → Instructions ajout

================================================================================

API ENDPOINTS

GET /telephony/{ba}/line → Liste lignes
GET /telephony/{ba}/line/{sn} → Détails ligne
GET /telephony/{ba}/line/{sn}/phone → Infos SIP
POST /telephony/{ba}/line/{sn}/changePassword → Nouveau mot de passe
GET /telephony/{ba}/line/{sn}/softphone/qrcode → QR code config
POST /telephony/{ba}/line/{sn}/softphone/qrcode/regenerate → Régénérer QR
GET /telephony/{ba}/line/{sn}/softphone/devices → Appareils connectés
DELETE /telephony/{ba}/line/{sn}/softphone/devices/{id} → Déconnecter appareil

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
