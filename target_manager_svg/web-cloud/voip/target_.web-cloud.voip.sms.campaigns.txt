================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.sms.campaigns.svg
Route:   /web-cloud/voip/sms/{accountId}/campaigns
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| sms-ab12345-1             | GET /sms → item                         |
| 3 actives                 | GET /sms/{account}/jobs?status=active → count |
| 2 programmées             | GET /sms/{account}/jobs?status=scheduled → count |
| 12 terminées              | GET /sms/{account}/jobs?status=done → count |
| 97.8%                     | GET /sms/{account}/statistics → avgDeliveryRate |
| Promo Janvier 2026        | GET /sms/{account}/jobs → name          |
| 1500                      | GET /sms/{account}/jobs/{id} → recipientsCount |
| 847/1500                  | GET /sms/{account}/jobs/{id} → sentCount |
| En cours/Programmé/Terminé| GET /sms/{account}/jobs/{id} → status   |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk] - SMS actif
   - Recherche
   - Liste des comptes SMS
4. RIGHT PANEL
   - Header: Identifiant compte + badge status
   - NAV4: [Général][Envoyer][Campagnes][Reçus][Modèles]
   - Titre: Campagnes SMS
   - Toolbar: Refresh, recherche, filtre statut, + Nouvelle campagne
   - Stats cards (4):
     * Campagnes actives
     * Programmées
     * Terminées ce mois
     * Taux moyen délivrabilité
   - Table campagnes:
     * Colonnes: Nom, Destinataires, Envoyés, Délivrés, Date, Statut, Crédits, Actions
     * Statuts: En cours (warning), Programmé (info), Terminé (success), Échec (error)
   - Pagination

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic compte → Charge les détails du compte
- Clic NAV4 → Navigation vers l'onglet
- Clic ↻ → Actualiser liste
- Clic Nouvelle campagne → Modal création
- Clic filtre statut → Dropdown filtrage
- Clic 👁 → Voir détails campagne
- Clic ⏸ → Mettre en pause (en cours)
- Clic ✎ → Modifier (programmé)
- Clic × → Supprimer/Annuler
- Clic 📊 → Statistiques détaillées
- Clic 🔄 → Relancer (échec)
- Clic ▶ → Lancer maintenant

================================================================================

API ENDPOINTS

GET /sms → Liste comptes SMS
GET /sms/{account}/jobs → Liste campagnes
GET /sms/{account}/jobs/{id} → Détails campagne
POST /sms/{account}/jobs → Créer campagne
PUT /sms/{account}/jobs/{id} → Modifier campagne
DELETE /sms/{account}/jobs/{id} → Supprimer campagne
POST /sms/{account}/jobs/{id}/start → Démarrer
POST /sms/{account}/jobs/{id}/pause → Pause
GET /sms/{account}/jobs/{id}/statistics → Stats campagne

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
