================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.number.config.svg
Route:   /web-cloud/voip/{billingAccount}/number/{serviceName}/config
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| 0033912345680         | Route param {serviceName}                     |
| Agent 1               | GET /telephony/{ba}/easyHunting/{sn}/agent → name |
| Jean Dupont           | agent → description                           |
| 0033912345678         | agent → number                                |
| Disponible/Pause/...  | agent → status                                |
| 20 sec                | agent → timeout                               |
| File 1, File 2        | GET /telephony/{ba}/easyHunting/{sn}/agent/{id}/queue |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL - Liste des numéros
4. RIGHT PANEL
   - Header: Numéro + badge + breadcrumb /Configuration
   - NAV3: Général | [Configuration] | Consommation | Statistiques
   - NAV4: [Agents] | Files | Planification | Options
   - Toolbar: Refresh + Ajouter un agent
   - Table agents
   - Pagination
   - Bannière info

# STATUTS AGENTS
| Statut      | Badge Color | Description                |
|-------------|-------------|----------------------------|
| Disponible  | #DEF7EC     | Prêt à recevoir des appels |
| En appel    | #DBEAFE     | Communication en cours     |
| Pause       | #FEF3C7     | Pause temporaire           |
| Déconnecté  | #F3F4F6     | Hors ligne                 |

# NAV4 SUB-TABS (File d'appels)
| Tab          | Contenu                                |
|--------------|----------------------------------------|
| Agents       | Liste agents, ligne associée, timeout  |
| Files        | Files d'attente, stratégie distribution|
| Planification| Horaires d'ouverture, jours fériés     |
| Options      | Musique d'attente, annonces            |

# INTERACTIONS
- Clic NAV4 sous-tab → Navigation config
- Clic "Ajouter un agent" → Modal ajout agent
- Clic "✎" → Modal édition agent
- Clic "🗑️" → Confirmation suppression agent
- Clic refresh → Actualiser statuts

# API ENDPOINTS
GET /telephony/{ba}/easyHunting/{sn}/agent              → Liste agents
POST /telephony/{ba}/easyHunting/{sn}/agent             → Créer agent
GET /telephony/{ba}/easyHunting/{sn}/agent/{id}         → Détail agent
PUT /telephony/{ba}/easyHunting/{sn}/agent/{id}         → Modifier agent
DELETE /telephony/{ba}/easyHunting/{sn}/agent/{id}      → Supprimer agent
GET /telephony/{ba}/easyHunting/{sn}/agent/{id}/queue   → Files de l'agent
GET /telephony/{ba}/easyHunting/{sn}/hunting/queue      → Liste files
PUT /telephony/{ba}/easyHunting/{sn}/hunting/queue/{id} → Modifier file

================================================================================
