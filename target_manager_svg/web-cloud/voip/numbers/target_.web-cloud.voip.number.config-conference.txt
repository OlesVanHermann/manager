================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.number.config-conference.svg
Route:   /web-cloud/voip/{billingAccount}/numbers/{serviceName}/config
Version: v1 (2025-12-30)
================================================================================

DONNÉES DYNAMIQUES
| Exemple SVG             | Source API                                      |
|-------------------------|-------------------------------------------------|
| 0033 9 72 10 80 00      | GET /telephony/{ba}/number/{sn} → serviceName   |
| Salle de conférence     | GET /telephony/{ba}/number/{sn} → description   |
| 6 numéros               | GET /telephony/{ba}/number → count              |
| 1234                    | GET /telephony/{ba}/conference/{sn} → pin       |
| Activé/Désactivé        | GET /telephony/{ba}/conference/{sn} → recordStatus|
| Français                | GET /telephony/{ba}/conference/{sn} → language  |
| URLs conference         | GET /telephony/{ba}/conference/{sn}/webAccess   |
| ab12345-ovh             | Session utilisateur                             |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif, x=510)
2. NAV2 - Web Cloud (VoIP actif, x=500)
3. LEFT PANEL - Liste des numéros du groupe (badge type: Conf, Queue, SVI...)
4. RIGHT PANEL
   - Header: Numéro + description + type (Conférence)
   - NAV3: 4 tabs (Général, **Configuration**, Portabilité, Statistiques)
   - NAV4: 3 sous-tabs (**Paramètres**, PIN, Participants) - spécifique conférence
   - Bloc gauche: Configuration conférence (PIN, annonce, enregistrement, micro, anonymes, langue)
   - Bloc droite haut: URLs de suivi et contrôle
   - Bloc droite bas: Administration (rapport email)

# INTERACTIONS
- Clic NAV3 tab → Navigation vers autre section numéro
- Clic NAV4 sous-tab → Bascule Paramètres/PIN/Participants
- Input PIN → Saisie code d'accès (optionnel)
- Bouton Télécharger → Upload fichier audio annonce
- Toggle options → Active/désactive fonctionnalités
- Select langue → Change langue conférence
- Bouton Générer URLs → Génère URLs de suivi/contrôle
- 📋 → Copie URL dans presse-papier
- Bouton Valider → Sauvegarde configuration

# API ENDPOINTS
GET  /telephony/{ba}/conference/{sn}                       # Config conférence
PUT  /telephony/{ba}/conference/{sn}                       # Modifier config
POST /telephony/{ba}/conference/{sn}/announceUpload        # Upload annonce
GET  /telephony/{ba}/conference/{sn}/webAccess             # URLs accès web
POST /telephony/{ba}/conference/{sn}/webAccess             # Générer URLs

# PARAMÈTRES CONFÉRENCE
- pin: string (code PIN optionnel)
- recordStatus: boolean (enregistrement)
- enterMuted: boolean (micro muet à l'entrée)
- anonymousRejection: boolean (rejeter anonymes)
- language: enum (fr, en, de, es, it, pl)
- reportStatus: enum (none, customer, manager)

================================================================================
