================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.line.consumption.svg
Route:   /web-cloud/voip/{billingAccount}/line/{serviceName}/consumption
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| 0033912345678         | Route param {serviceName}                     |
| 127                   | GET /telephony/{ba}/line/{sn}/statistics → callsCount |
| 5h 34min              | statistics → totalDuration (seconds → format) |
| 12,50€                | statistics → totalCost                        |
| 2,30€                 | statistics → outOfPlanCost                    |
| 30/12 14:32           | GET /telephony/{ba}/line/{sn}/calls → datetime |
| +33 6 12 34 56 78     | calls → called                                |
| Mobile France         | calls → destinationType                       |
| 5:23                  | calls → duration (seconds → mm:ss)            |
| 0,00€                 | calls → priceWithoutTax                       |
| Forfait/Hors forfait  | calls → planType                              |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL - Liste des lignes
4. RIGHT PANEL
   - Header: Numéro + breadcrumb /Consommation
   - NAV3: Général | Téléphone | Appels | Transferts | Messagerie | [Consommation] | Options
   - NAV4: [Appels émis] | Appels reçus | Fax émis | Fax reçus
   - Sélecteur période (mois)
   - Cards stats: Total appels | Durée totale | Coût total | Hors forfait
   - Table détail appels
   - Pagination
   - Bouton Export CSV

# TYPES TARIFICATION
| Type         | Badge Color | Description            |
|--------------|-------------|------------------------|
| Forfait      | #DEF7EC     | Inclus dans l'offre    |
| Hors forfait | #FEE2E2     | Facturé en supplément  |
| Gratuit      | #F3F4F6     | Numéro vert/gratuit    |

# INTERACTIONS
- Clic NAV4 sous-tab → Filtrer par type (émis/reçus, appels/fax)
- Sélecteur période → Changer mois de consultation
- Pagination → Navigation pages
- Clic "📄" → Détail appel
- Clic "Exporter CSV" → Téléchargement fichier

# API ENDPOINTS
GET /telephony/{ba}/line/{sn}/statistics            → Statistiques agrégées
GET /telephony/{ba}/line/{sn}/calls                 → Liste appels
GET /telephony/{ba}/line/{sn}/calls?wayType=outgoing → Appels émis
GET /telephony/{ba}/line/{sn}/calls?wayType=incoming → Appels reçus
GET /telephony/{ba}/service/{sn}/faxConsumption     → Consommation fax

================================================================================
