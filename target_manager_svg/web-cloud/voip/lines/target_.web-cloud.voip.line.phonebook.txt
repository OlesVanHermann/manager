================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.line.phonebook.svg
Route:   /web-cloud/voip/{billingAccount}/line/{serviceName}/phonebook
Version: v1 (2024-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                          |
|-----------------------|-----------------------------------------------------|
| 0033972101234         | GET /telephony/{ba}/line/{sn} → serviceName         |
| Bureau Direction      | GET /telephony/{ba}/line/{sn} → description         |
| Contacts table        | GET /telephony/{ba}/line/{sn}/phonebook/contacts    |
| 12 contacts           | GET /telephony/{ba}/line/{sn}/phonebook → count     |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - VoIP actif
3. LEFT PANEL - Liste des lignes du groupe
4. RIGHT PANEL
   - Header: Numéro + description
   - NAV3: Tabs (Répertoire actif)
   - Toolbar: Actions (Ajouter, Importer, Exporter)
   - Table: Liste contacts
   - Pagination

# TABLE CONTACTS
| Colonne | Source |
|---------|--------|
| Nom | contact.name |
| Téléphone | contact.phone |
| Mobile | contact.mobile |
| Email | contact.email |

# TOOLBAR
- Refresh (↻)
- Recherche contacts
- + Ajouter → Modal ajout contact
- 📥 Importer → Import CSV
- 📤 Exporter → Export CSV/vCard

# INTERACTIONS
- "Ajouter" → Modal création contact
- "Importer" → Modal import CSV
- "Exporter" → Télécharge fichier
- "Modifier" → Modal édition contact
- "Supprimer" → Confirmation suppression
- Pagination → Navigation pages

# API ENDPOINTS
GET /telephony/{ba}/line/{sn}/phonebook
GET /telephony/{ba}/line/{sn}/phonebook/contacts
POST /telephony/{ba}/line/{sn}/phonebook/contacts
PUT /telephony/{ba}/line/{sn}/phonebook/contacts/{id}
DELETE /telephony/{ba}/line/{sn}/phonebook/contacts/{id}
POST /telephony/{ba}/line/{sn}/phonebook/import
GET /telephony/{ba}/line/{sn}/phonebook/export

================================================================================
