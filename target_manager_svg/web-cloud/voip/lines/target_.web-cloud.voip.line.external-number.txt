================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.line.external-number.svg
Route:   /web-cloud/voip/{billingAccount}/line/{serviceName}/external-number
Version: v1 (2024-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG               | Source API                                               |
|---------------------------|----------------------------------------------------------|
| +33 6 12 34 56 78         | GET /telephony/{ba}/line/{sn}/options → externalNumber  |
| Vérifié                   | GET /telephony/{ba}/line/{sn}/options → externalStatus  |
| 15/10/2024                | GET /telephony/{ba}/line/{sn}/options → verifiedDate    |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - VoIP actif
3. LEFT PANEL - Liste des lignes du groupe
4. RIGHT PANEL
   - Back link: Retour aux options
   - Titre: Numéro externe
   - Sections: Actuel, Modifier, Vérification

# SECTIONS
1. Numéro externe actuel
   - Numéro configuré
   - Statut (badge Vérifié)
   - Date vérification

2. Modifier le numéro
   - Input nouveau numéro
   - Bouton envoyer code SMS

3. Vérification
   - Input code SMS
   - Bouton vérifier

4. Actions
   - Supprimer numéro externe

# INTERACTIONS
- Input numéro → Saisie format international
- "Envoyer code SMS" → Envoi SMS vérification
- "Vérifier" → Valide le code
- "Supprimer" → Confirmation suppression

# API ENDPOINTS
GET /telephony/{ba}/line/{sn}/options
PUT /telephony/{ba}/line/{sn}/options
POST /telephony/{ba}/line/{sn}/externalNumber/verify
DELETE /telephony/{ba}/line/{sn}/externalNumber

================================================================================
