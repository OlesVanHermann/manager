================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.fax.filtering.svg
Route:   /web-cloud/voip/{billingAccount}/fax/{serviceName}/filtering
Version: v1 (2024-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG               | Source API                                        |
|---------------------------|---------------------------------------------------|
| Liste règles              | GET /telephony/{ba}/fax/{sn}/screenLists          |
| Stats bloqués             | GET /telephony/{ba}/fax/{sn}/consumption/blocked  |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section Web Cloud (VoIP actif)
3. LEFT PANEL - Liste des fax du groupe
4. RIGHT PANEL
   - Header: "Filtrage des fax"
   - NAV3: Tabs fax (Filtrage actif)
   - Toolbar: Refresh + Ajouter règle
   - Bannière info
   - Table des règles
   - Mode par défaut
   - Stats filtrage

# TYPES DE RÈGLES
| Type | Badge | Description |
|------|-------|-------------|
| Bloqué | Rouge | Numéros interdits |
| Autorisé | Vert | Numéros autorisés (liste blanche) |

# TABLE RÈGLES
| Colonne | Description |
|---------|-------------|
| Type | Bloqué / Autorisé |
| Numéro / Préfixe | Pattern (* = joker) |
| Description | Note libre |
| Statut | Actif / Inactif |
| Actions | Modifier / Supprimer |

# MODES PAR DÉFAUT
| Mode | Description |
|------|-------------|
| Tout accepter | Accepte tout sauf liste noire |
| Tout refuser | Refuse tout sauf liste blanche |

# INTERACTIONS
- "+ Ajouter règle" → Modal création règle
- "✎ Modifier" → Modal édition règle
- "×" → Modal confirmation suppression
- Radio mode → PUT API changement mode

# API ENDPOINTS
GET /telephony/{ba}/fax/{sn}/screenLists
POST /telephony/{ba}/fax/{sn}/screenLists
PUT /telephony/{ba}/fax/{sn}/screenLists/{id}
DELETE /telephony/{ba}/fax/{sn}/screenLists/{id}

================================================================================
