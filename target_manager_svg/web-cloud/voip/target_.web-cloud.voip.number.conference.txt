================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.number.conference.svg
Route:   /web-cloud/voip/number/{serviceNumber}/conference
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| +33 1 23 45 67 00         | GET /telephony/{ba}/number → item       |
| Conférence                | GET /telephony/{ba}/number/{sn} → description |
| ••••••                    | GET /telephony/{ba}/conference/{sn} → pin |
| 50                        | GET /telephony/{ba}/conference/{sn} → maxParticipants |
| Activé                    | GET /telephony/{ba}/conference/{sn} → recordStatus |
| ON/OFF                    | GET /telephony/{ba}/conference/{sn} → settings |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk]
   - Recherche
   - Liste des numéros
4. RIGHT PANEL
   - Header: Numéro + description + badge status
   - NAV4: [Général][Participants][Historique]
   - Card infos conférence:
     * Numéro d'accès, Type, Code PIN, Participants max, Enregistrement
   - Bannière info conférence
   - Tiles paramètres:
     * Annonce d'entrée (ON/OFF)
     * Annonce de sortie (ON/OFF)
     * Musique d'attente (ON/OFF)
     * Langue des annonces (éditable)
     * Mode anonyme (ON/OFF)
     * Supprimer (danger)

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic numéro → Charge les détails du numéro
- Clic NAV4 → Navigation vers l'onglet
- Clic 👁 Code PIN → Affiche/masque le code
- Clic 📋 → Copie dans le presse-papier
- Clic ✎ Langue → Modal changement langue
- Clic ON/OFF → Toggle paramètre
- Clic Supprimer → Modal confirmation suppression

================================================================================

API ENDPOINTS

GET /telephony/{ba}/number → Liste numéros
GET /telephony/{ba}/number/{sn} → Détails numéro
GET /telephony/{ba}/conference/{sn} → Configuration conférence
PUT /telephony/{ba}/conference/{sn} → Modifier configuration
GET /telephony/{ba}/conference/{sn}/histories → Historique conférences
GET /telephony/{ba}/conference/{sn}/participants → Liste participants
POST /telephony/{ba}/conference/{sn}/lock → Verrouiller conférence
DELETE /telephony/{ba}/number/{sn}/changeFeatureType → Supprimer config

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
