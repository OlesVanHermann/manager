================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.sms.general.svg
Route:   /web-cloud/voip/sms/{accountId}
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| sms-ab12345-1             | GET /sms → item                         |
| 1 250                     | GET /sms/{account} → creditsLeft        |
| 847                       | GET /sms/{account}/outgoing → count(30d)|
| 98.5%                     | GET /sms/{account}/statistics → rate    |
| 3                         | GET /sms/{account}/jobs → activeCount   |
| ENTREPRISE                | GET /sms/{account} → senderForResponse  |
| 15/03/2024                | GET /sms/{account} → creationDate       |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk] - SMS actif
   - Recherche
   - Liste des comptes SMS avec crédits
4. RIGHT PANEL
   - Header: Identifiant compte + badge status
   - NAV4: [Général][Envoyer][Campagnes][Reçus][Modèles]
   - Stats cards (4):
     * Crédits disponibles
     * SMS envoyés (30j)
     * Taux de délivrabilité
     * Campagnes actives
   - Bannière warning crédits faibles
   - Section Détails du compte:
     * Identifiant (copiable)
     * Description (modifiable)
     * Expéditeur par défaut (modifiable)
     * Dates création/activité
   - Section Actions rapides:
     * Envoyer un SMS
     * Nouvelle campagne
     * Acheter des crédits
     * Gérer les modèles
   - Section Activité récente (3 lignes)

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic compte → Charge les détails du compte
- Clic NAV4 → Navigation vers l'onglet
- Clic Recharger → Modal achat crédits
- Clic 📋 → Copier identifiant
- Clic Modifier description → Modal édition
- Clic Modifier expéditeur → Modal édition
- Clic action rapide → Navigation ou modal
- Clic Voir tout → Navigation historique complet

================================================================================

API ENDPOINTS

GET /sms → Liste comptes SMS
GET /sms/{account} → Détails compte
PUT /sms/{account} → Modifier description/sender
GET /sms/{account}/outgoing → SMS envoyés
GET /sms/{account}/incoming → SMS reçus
GET /sms/{account}/jobs → Campagnes
GET /sms/{account}/statistics → Statistiques
POST /sms/{account}/creditsOrder → Commander crédits

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
