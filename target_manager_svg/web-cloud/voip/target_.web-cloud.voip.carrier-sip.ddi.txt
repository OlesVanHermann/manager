================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.carrier-sip.ddi.svg
Route:   /web-cloud/voip/carrier-sip/{trunkId}/ddi
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG               | Source API                              |
|---------------------------|-----------------------------------------|
| trunk-ab12345-1           | GET /telephony/{ba}/carrierSip → item   |
| 12 sur 20                 | GET /telephony/{ba}/carrierSip/{id}/ddi → count/max |
| +33 1 84 XX XX 00         | GET /telephony/{ba}/carrierSip/{id}/ddi → number |
| Accueil standard          | GET /telephony/{ba}/carrierSip/{id}/ddi/{n} → description |
| SVI → Menu principal      | GET /telephony/{ba}/carrierSip/{id}/ddi/{n} → destination |
| Principal/SDA/Fax/Plage   | GET /telephony/{ba}/carrierSip/{id}/ddi/{n} → type |
| Actif/Inactif             | GET /telephony/{ba}/carrierSip/{id}/ddi/{n} → status |

================================================================================

STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Web Cloud (VoIP actif)
3. LEFT PANEL
   - NAV3: [SIP][SMS][FAX][Trunk] - Trunk actif
   - Recherche
   - Liste des trunks SIP
4. RIGHT PANEL
   - Header: ID trunk + description + badge status
   - NAV4: [Général][Paramètres][Routage][CDR][DDI]
   - Titre: Numéros DDI
   - Bannière info (compteur DDI, plage disponible)
   - Toolbar: Refresh, recherche, filtre statut, Commander DDI, Ajouter existant
   - Table DDI:
     * Colonnes: Checkbox, Numéro DDI, Description, Destination, Type, Statut, Actions
     * Types: Principal (bleu), SDA (gris), Fax (warning), Plage (gris)
     * Statuts: Actif (success), Inactif (gris)
   - Pagination

================================================================================

INTERACTIONS

- Clic NAV3 → Change le type de service (SIP/SMS/FAX/Trunk)
- Clic trunk → Charge les détails du trunk
- Clic NAV4 → Navigation vers l'onglet
- Clic ↻ → Actualiser liste
- Clic Commander DDI → Tunnel commande nouveaux numéros
- Clic Ajouter existant → Modal attribution DDI existant
- Clic checkbox → Sélection multiple
- Clic ✎ → Modal édition (description, destination)
- Clic 📋 → Copier numéro
- Clic × → Modal confirmation suppression/désattribution

================================================================================

API ENDPOINTS

GET /telephony/{ba}/carrierSip → Liste trunks
GET /telephony/{ba}/carrierSip/{id}/ddi → Liste DDI
GET /telephony/{ba}/carrierSip/{id}/ddi/{number} → Détails DDI
POST /telephony/{ba}/carrierSip/{id}/ddi → Attribuer DDI
PUT /telephony/{ba}/carrierSip/{id}/ddi/{number} → Modifier DDI
DELETE /telephony/{ba}/carrierSip/{id}/ddi/{number} → Désattribuer DDI
POST /telephony/{ba}/order/ddi → Commander nouveaux DDI

================================================================================

DIMENSIONS CONFORMITE

| Element       | Valeur   | Conforme |
|---------------|----------|----------|
| NAV1 height   | 50px     | OK       |
| NAV2 height   | 40px     | OK       |
| Gap NAV2→     | 15px     | OK       |
| LEFT PANEL    | 280px    | OK       |
| Gap panels    | 16px     | OK       |
| RIGHT PANEL   | 1096px   | OK       |

================================================================================
