================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.offer.offer.svg
Route:   /web-cloud/hosting/{serviceName}/offer/offer
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| Performance 1            | GET /hosting/web/{sn} → offer                    |
| 01/01/2024               | GET /hosting/web/{sn}/serviceInfos → creation    |
| 01/02/2026               | GET /hosting/web/{sn}/serviceInfos → expiration  |
| 9,99 EUR                 | GET /order/catalog/public → pricing.price        |
| 250 Go                   | GET /hosting/web/{sn} → quotaSize                |
| 5                        | GET /hosting/web/{sn} → databaseNumber.max       |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General Sites [Offre] Expert
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: [Offre] Changement Migration Resiliation
   - Contenu:
     - Bloc Offre actuelle (formule, dates, prix, specs)
     - Bloc Changer de formule (4 cards offres)
     - Bloc Options supplementaires (CDN, SSL, Boost)

INTERACTIONS
- Clic Passer a cette offre -> Modal confirmation upgrade
- Clic Configurer CDN -> Navigation vers /sites/cdn
- Clic Activer Boost -> Modal activation boost

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/serviceInfos         # Infos service (dates, facturation)
GET /order/catalog/public/webHosting       # Catalogue tarifs
GET /order/upgrade/webHosting/{sn}         # Offres upgrade disponibles
POST /order/upgrade/webHosting/{sn}/{plan} # Commander upgrade

================================================================================
