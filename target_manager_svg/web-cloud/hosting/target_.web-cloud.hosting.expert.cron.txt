================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.cron.svg
Route:   /web-cloud/hebergement/hosting/{serviceName}/cron
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG              | Source API                                  |
|--------------------------|---------------------------------------------|
| cron/backup.php          | GET /hosting/web/{sn}/cron → command        |
| PHP 8.2                  | GET /hosting/web/{sn}/cron/{id} → language  |
| 0 2 * * *                | GET /hosting/web/{sn}/cron/{id} → frequency |
| admin@mywebsite.com      | GET /hosting/web/{sn}/cron/{id} → email     |
| Activé/Désactivé         | GET /hosting/web/{sn}/cron/{id} → state     |
| Backup quotidien         | GET /hosting/web/{sn}/cron/{id} → description|
================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Hébergement actif
3. LEFT PANEL - Liste hébergements
4. RIGHT PANEL
   - Header: Nom service
   - NAV3: 16 tabs (Cron actif)
   - TITRE + BOUTON AJOUTER
   - TABLE CRON JOBS
   - PAGINATION
   - BANNIÈRE AIDE

# COLONNES TABLE
| Colonne     | Largeur | Contenu                          |
|-------------|---------|----------------------------------|
| Commande    | 200px   | Script à exécuter (monospace)    |
| Langage     | 90px    | Badge PHP 8.x / Autre            |
| Fréquence   | 140px   | Expression cron (monospace)      |
| E-mail      | 190px   | Adresse notification ou "Non"    |
| État        | 100px   | Badge Activé/Désactivé           |
| Description | 180px   | Note courte                      |
| Actions     | 56px    | Menu ⋮                           |

# EXPRESSIONS CRON
| Expression    | Signification                    |
|---------------|----------------------------------|
| 0 2 * * *     | Tous les jours à 2h              |
| */15 * * * *  | Toutes les 15 minutes            |
| 0 8 * * 1     | Tous les lundis à 8h             |
| 0 4 * * *     | Tous les jours à 4h              |

# LANGAGES SUPPORTÉS
| Langage | Badge couleur |
|---------|---------------|
| PHP 8.x | Bleu #DBEAFE  |
| Autre   | Gris #F3F4F6  |

# MENU ACTIONS
- Modifier
- Supprimer (rouge)

# INTERACTIONS
- Clic "+ Ajouter une planification" → Modal création
- Clic ⋮ → Menu (Modifier, Supprimer)
- Clic "Consulter nos guides" → Lien externe

# API ENDPOINTS
GET /hosting/web/{sn}/cron              - Liste des crons
GET /hosting/web/{sn}/cron/{id}         - Détails cron
POST /hosting/web/{sn}/cron             - Créer cron
PUT /hosting/web/{sn}/cron/{id}         - Modifier cron
DELETE /hosting/web/{sn}/cron/{id}      - Supprimer cron

================================================================================
