================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.runtimes.svg
Route:   /web-cloud/hebergement/hosting/{serviceName}/runtimes
Version: v1 (2025-12-30)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                     | Source API                              |
|---------------------------------|-----------------------------------------|
| php8.2-default                  | GET /hosting/web/{sn}/runtime → name    |
| PHP 8.2                         | GET /hosting/web/{sn}/runtime/{id} → type |
| 5                               | GET /hosting/web/{sn}/runtime/{id} → attachedDomains |
| ✓ (défaut)                     | GET /hosting/web/{sn}/runtime/{id} → isDefault |
| Créé/Suppression               | GET /hosting/web/{sn}/runtime/{id} → status |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Hébergement actif
3. LEFT PANEL - Liste hébergements (280px)
4. RIGHT PANEL (1096px)
   - Header: {serviceName}
   - NAV3: 16 tabs, Runtimes actif
   - Description
   - TOOLBAR: + Ajouter runtime
   - TABLE: Défaut, Nom, Langage, Multisites associés, État, Actions
   - PAGINATION

# LANGAGES
- PHP 7.4, 8.0, 8.1, 8.2, 8.3
- Node.js 16, 18, 20

# STATUTS
- Créé (vert) - Runtime actif
- Suppression (rouge) - Runtime en cours de suppression

# ACTIONS
- + Ajouter runtime → modal-runtime-add
- ⋮ (row action) → Modifier / Définir par défaut / Supprimer

# API ENDPOINTS
GET /hosting/web/{serviceName}/runtime
GET /hosting/web/{serviceName}/runtime/{id}
POST /hosting/web/{serviceName}/runtime
PUT /hosting/web/{serviceName}/runtime/{id}
DELETE /hosting/web/{serviceName}/runtime/{id}

================================================================================
