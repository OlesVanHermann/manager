================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.sites.ssl.svg
Route:   /web-cloud/hosting/{serviceName}/sites/ssl
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| Let's Encrypt (gratuit)  | GET /hosting/web/{sn}/ssl → provider             |
| Actif                    | GET /hosting/web/{sn}/ssl → status               |
| 15/03/2026               | GET /hosting/web/{sn}/ssl → expiration           |
| Renouvellement auto      | GET /hosting/web/{sn}/ssl → regenerable          |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General [Sites] Offre Expert
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: Multisite [SSL] FTP-SSH BDD CDN
   - Contenu:
     - Banniere info SSL
     - Bloc Statut certificat (type, statut, domaine, expiration, renouvellement)
     - Actions: Regenerer, Commander, Supprimer
     - Table domaines couverts
     - Pagination

INTERACTIONS
- Bouton Regenerer SSL -> Regeneration certificat
- Bouton Commander SSL -> Modal commande certificat payant
- Bouton Supprimer SSL -> Modal confirmation suppression
- Clic Regenerer domaine -> Regeneration pour ce domaine specifique

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/ssl                  # Details certificat SSL
POST /hosting/web/{sn}/ssl                 # Commander/Regenerer SSL
DELETE /hosting/web/{sn}/ssl               # Supprimer SSL
GET /hosting/web/{sn}/ssl/domains          # Domaines couverts
POST /hosting/web/{sn}/ssl/regenerate      # Regenerer certificat

================================================================================
