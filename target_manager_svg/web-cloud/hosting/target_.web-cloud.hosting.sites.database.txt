================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.sites.database.svg
Route:   /web-cloud/hosting/{serviceName}/sites/database
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                        | Source API                                    |
|------------------------------------|-----------------------------------------------|
| monsite.example.com                | GET /hosting/web → serviceName                |
| monsite_db1                        | GET /hosting/web/{sn}/database → name         |
| mysql51-123.hosting.ovh.net        | GET /hosting/web/{sn}/database/{db} → server  |
| MySQL 8.0                          | GET /hosting/web/{sn}/database/{db} → version |
| 400 Mo                             | GET /hosting/web/{sn}/database/{db} → quotaSize |
| 45%                                | GET /hosting/web/{sn}/database/{db} → quotaUsed |
| 3 / 5                              | GET /hosting/web/{sn} → databaseNumber.used/max |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General [Sites] Offre Expert
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: Multisite SSL FTP-SSH [BDD] CDN
   - Contenu:
     - Banniere info bases de donnees
     - Toolbar: Refresh, Recherche, + Creer une base
     - Table bases: Nom, Serveur, Version, Quota, Utilisation, Statut, Actions
     - Pagination
     - Bloc Quota (bases utilisees, espace total)
     - Bloc Informations connexion (serveur, port)

INTERACTIONS
- Bouton + Creer une base -> Modal creation base de donnees
- Clic phpMyAdmin -> Ouverture phpMyAdmin (nouvel onglet)
- Clic 🔑 -> Modal changer mot de passe
- Clic × -> Modal confirmation suppression
- Bouton Commander Cloud DB -> Redirection commande Cloud DB

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/database             # Liste bases de donnees
GET /hosting/web/{sn}/database/{name}      # Details base
POST /hosting/web/{sn}/database            # Creer base
DELETE /hosting/web/{sn}/database/{name}   # Supprimer base
POST /hosting/web/{sn}/database/{name}/changePassword # Changer mot de passe
GET /hosting/web/{sn}/database/{name}/dump # Obtenir dump

================================================================================
