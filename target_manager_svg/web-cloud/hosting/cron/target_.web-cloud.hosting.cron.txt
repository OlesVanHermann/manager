================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.cron.svg
Route:   /web-cloud/hosting/{serviceName}/cron
Version: v1 (2025-12-29)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                              |
|--------------------------|-----------------------------------------|
| Nettoyage cache          | GET /hosting/web/{sn}/cron → description|
| php www/cron/clean.php   | GET /hosting/web/{sn}/cron → command    |
| Toutes les heures        | GET /hosting/web/{sn}/cron → frequency  |
| Actif                    | GET /hosting/web/{sn}/cron → status     |
| 29/12 14:00 ✓            | GET /hosting/web/{sn}/cron → lastExecution |
================================================================================

# STRUCTURE PAGE
1. LEFT PANEL - Hébergement + NAV3 (CRON active)
2. RIGHT PANEL:
   - Titre "Tâches planifiées (CRON)"
   - INFO BANNER: Explication CRON
   - TOOLBAR: Refresh, Bouton Ajouter
   - TABLE: Description, Commande, Fréquence, Statut, Dernière exec, Actions
   - PAGINATION

# FRÉQUENCES DISPONIBLES
- Toutes les 5/10/15/30 minutes
- Toutes les heures
- Tous les jours à Xh
- Toutes les semaines
- Tous les mois
- Expression CRON personnalisée

# API ENDPOINTS
GET /hosting/web/{serviceName}/cron - Liste tâches CRON
POST /hosting/web/{serviceName}/cron - Créer tâche
PUT /hosting/web/{serviceName}/cron/{id} - Modifier
DELETE /hosting/web/{serviceName}/cron/{id} - Supprimer
================================================================================
