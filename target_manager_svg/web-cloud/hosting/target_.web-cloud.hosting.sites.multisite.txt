================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.sites.multisite.svg
Route:   /web-cloud/hosting/{serviceName}/sites/multisite
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                          |
|--------------------------|-----------------------------------------------------|
| monsite.example.com      | GET /hosting/web/{sn}/attachedDomain → domain       |
| /blog                    | GET /hosting/web/{sn}/attachedDomain/{d} → path     |
| SSL Actif                | GET /hosting/web/{sn}/attachedDomain/{d} → ssl      |
| Firewall ON              | GET /hosting/web/{sn}/attachedDomain/{d} → firewall |
| CDN Actif                | GET /hosting/web/{sn}/attachedDomain/{d} → cdn      |
| OK                       | GET /hosting/web/{sn}/attachedDomain/{d} → status   |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General [Sites] Offre Expert
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: [Multisite] SSL FTP-SSH BDD CDN
   - Contenu:
     - Toolbar: Refresh, Recherche, + Ajouter domaine
     - Table: Domaine, Chemin, SSL, Firewall, CDN, Statut, Actions
     - Pagination

INTERACTIONS
- Bouton + Ajouter domaine -> Modal ajout (OVH ou externe)
- Clic Modifier -> Modal modification domaine
- Clic Diag. -> Modal diagnostic DNS/SSL
- Clic Suppr. -> Modal confirmation suppression
- Clic CDN -> Vider cache CDN pour ce domaine

API ENDPOINTS
GET /hosting/web/{sn}/attachedDomain              # Liste domaines attaches
GET /hosting/web/{sn}/attachedDomain/{domain}     # Details domaine
POST /hosting/web/{sn}/attachedDomain             # Ajouter domaine
PUT /hosting/web/{sn}/attachedDomain/{domain}     # Modifier domaine
DELETE /hosting/web/{sn}/attachedDomain/{domain}  # Supprimer domaine
POST /hosting/web/{sn}/request                    # Diagnostic

================================================================================
