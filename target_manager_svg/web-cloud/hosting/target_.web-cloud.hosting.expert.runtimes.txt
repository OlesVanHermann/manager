================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.expert.runtimes.svg
Route:   /web-cloud/hosting/{serviceName}/expert/runtimes
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| default                  | GET /hosting/web/{sn}/runtime → name             |
| /                        | GET /hosting/web/{sn}/runtime/{id} → attachedDomains |
| PHP 8.2                  | GET /hosting/web/{sn}/runtime/{id} → phpVersion  |
| php-fpm                  | GET /hosting/web/{sn}/runtime/{id} → type        |
| legacy-app               | GET /hosting/web/{sn}/runtime → name             |
| api-v2                   | GET /hosting/web/{sn}/runtime → name             |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General Sites Offre [Expert]
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: General Boost Indy Emails Variables OvhConfig [Runtimes] Cron +2
   - Contenu:
     - Banniere info runtimes personnalises
     - Toolbar: Refresh, Recherche, + Creer runtime
     - Table: Nom, Chemin, Version PHP, Moteur, Statut, Actions
     - Pagination
     - Bloc Info fonctionnement runtimes

INTERACTIONS
- Bouton + Creer runtime -> Modal creation runtime
- Clic ✎ -> Modal modification runtime
- Clic × -> Modal confirmation suppression (sauf default)
- Badge "Principal" -> Runtime par defaut (non supprimable)

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/runtime              # Liste runtimes
GET /hosting/web/{sn}/runtime/{id}         # Details runtime
POST /hosting/web/{sn}/runtime             # Creer runtime
PUT /hosting/web/{sn}/runtime/{id}         # Modifier runtime
DELETE /hosting/web/{sn}/runtime/{id}      # Supprimer runtime

================================================================================
