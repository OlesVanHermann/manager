================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.expert.tasks.svg
Route:   /web-cloud/hosting/{serviceName}/expert/tasks
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| #12456789                | GET /hosting/web/{sn}/tasks → id                 |
| Deploiement FTP          | GET /hosting/web/{sn}/tasks/{id} → function      |
| 02/01/2026 14:32         | GET /hosting/web/{sn}/tasks/{id} → startDate     |
| done/doing/error         | GET /hosting/web/{sn}/tasks/{id} → status        |
| 67%                      | GET /hosting/web/{sn}/tasks/{id} → doneDate (calcul) |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General Sites Offre [Expert]
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: General Boost Indy Emails Variables OvhConfig Runtimes Cron [Taches] +1
   - Contenu:
     - Banniere info taches
     - Toolbar: Refresh, Recherche, Filtre statut
     - Table: ID, Type, Date creation, Date fin, Statut, Progression
     - Pagination
     - Bloc Info types de taches

INTERACTIONS
- Bouton ↻ -> Rafraichit la liste des taches
- Filtre statut -> Filtre Tous/En cours/Termine/Erreur
- Clic ligne -> Details de la tache (read-only)
- Progression -> Barre de progression animee pour taches en cours

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/tasks                # Liste taches
GET /hosting/web/{sn}/tasks/{id}           # Details tache

NOTE: Les taches sont en lecture seule, pas de creation/modification manuelle.
Les taches sont generees automatiquement lors d'operations (FTP, BDD, SSL...).

================================================================================
