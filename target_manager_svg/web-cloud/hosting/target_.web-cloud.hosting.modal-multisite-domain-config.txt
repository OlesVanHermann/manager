================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.modal-multisite-domain-config.svg
Route:   /web-cloud/hosting/{serviceName}/multisite/{domain} (modal DNS config)
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                  | Source API                                    |
|------------------------------|-----------------------------------------------|
| Domaine                      | GET /hosting/web/{sn}/attachedDomain/{domain} |
| Enregistrements requis       | attachedDomain → dnsConfiguration             |
| Statut verification          | attachedDomain → dnsStatus                    |
| IP serveur                   | GET /hosting/web/{sn} → hostingIp             |

================================================================================

STRUCTURE MODAL
1. Header avec titre
2. Affichage domaine
3. Tableau enregistrements DNS
4. Verification automatique
5. Banniere info propagation
6. Export configuration
7. Footer avec bouton fermer

ENREGISTREMENTS DNS
| Type  | Usage                              |
|-------|------------------------------------|
| A     | Pointer vers IP hebergement        |
| CNAME | Alias vers cluster                 |
| MX    | Configuration email                |
| TXT   | Verification SSL (ACME challenge)  |

STATUTS ENREGISTREMENT
| Statut     | Couleur | Description            |
|------------|---------|------------------------|
| OK         | Vert    | Correctement configure |
| En attente | Orange  | Non detecte encore     |
| Erreur     | Rouge   | Mauvaise configuration |

VERIFICATION
- Automatique toutes les 5 min
- Bouton verification manuelle
- Countdown jusqu'a prochaine verif

EXPORT
- Fichier texte avec tous records
- Format zone DNS standard
- Copie facile des valeurs

BOUTONS FOOTER
| Bouton  | Action           |
|---------|------------------|
| Fermer  | Fermer modal     |

INTERACTIONS
- Clic 📋 → Copier valeur
- Clic Verifier → Force check
- Clic Telecharger → Export DNS

API ENDPOINTS
GET /hosting/web/{sn}/attachedDomain/{domain}     → Config requise
GET /hosting/web/{sn}/attachedDomain/{domain}/dnsCheck → Verification
GET /hosting/web/{sn}/attachedDomain/{domain}/dnsExport → Export

================================================================================
