================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.expert.envvars.svg
Route:   /web-cloud/hosting/{serviceName}/expert/envvars
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| DATABASE_URL             | GET /hosting/web/{sn}/envVar → key               |
| mysql://user:***...      | GET /hosting/web/{sn}/envVar/{key} → value       |
| string/password/boolean  | GET /hosting/web/{sn}/envVar/{key} → type        |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General Sites Offre [Expert]
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: General Boost Indy Emails [Variables] OvhConfig Runtimes Cron +2
   - Contenu:
     - Banniere info variables environnement
     - Toolbar: Refresh, Recherche, + Ajouter variable
     - Table: Nom, Valeur (masquee si password), Type, Actions
     - Pagination
     - Bloc Info utilisation PHP (exemple code)

INTERACTIONS
- Bouton + Ajouter variable -> Modal creation variable
- Clic 👁 -> Affiche/masque valeur password
- Clic ✎ -> Modal modification variable
- Clic × -> Modal confirmation suppression

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement
GET /hosting/web/{sn}/envVar               # Liste variables
GET /hosting/web/{sn}/envVar/{key}         # Details variable
POST /hosting/web/{sn}/envVar              # Creer variable
PUT /hosting/web/{sn}/envVar/{key}         # Modifier variable
DELETE /hosting/web/{sn}/envVar/{key}      # Supprimer variable

================================================================================
