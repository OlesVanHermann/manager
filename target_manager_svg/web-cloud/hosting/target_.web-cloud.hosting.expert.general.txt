================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.expert.general.svg
Route:   /web-cloud/hosting/{serviceName}/expert
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                  | Source API                                    |
|------------------------------|-----------------------------------------------|
| {nicHandle}                  | Session utilisateur                           |
| monsite.example.com          | GET /hosting/web/{sn}                         |
| Version PHP                  | GET /hosting/web/{sn} → phpVersion            |
| Container                    | GET /hosting/web/{sn} → container             |
| Cluster                      | GET /hosting/web/{sn} → cluster               |
| Datacenter                   | GET /hosting/web/{sn} → datacenter            |
| IPv4/IPv6                    | GET /hosting/web/{sn} → ip, ipv6              |
| Nombre runtimes              | GET /hosting/web/{sn}/runtime → count         |
| Nombre envvars               | GET /hosting/web/{sn}/envVar → count          |
| Nombre cron                  | GET /hosting/web/{sn}/cron → count            |
| Options                      | GET /hosting/web/{sn}/serviceInfos            |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Hebergement actif
3. LEFT PANEL (280px)
   - NAV3 Groupement: [General][Sites][Offre][Expert] - Expert actif
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre
   - NAV4: 10 tabs Expert (General actif)
   - Cards resume (PHP, Runtimes, Variables, Cron)
   - Bloc configuration serveur
   - Bloc options activees

CARDS RESUME
| Card      | Contenu                           |
|-----------|-----------------------------------|
| PHP       | Version + container               |
| Runtimes  | Nombre + liste types              |
| Variables | Nombre de variables env           |
| Cron      | Nombre de taches planifiees       |

CONFIGURATION SERVEUR
| Champ         | Contenu                           |
|---------------|-----------------------------------|
| Cluster       | Nom du cluster                    |
| Datacenter    | Localisation (GRA, SBG...)        |
| IPv4          | Adresse IPv4 + copie              |
| IPv6          | Adresse IPv6 + copie              |
| Firewall HTTP | Statut ON/OFF                     |
| SSH           | Statut ON/OFF                     |
| Git deploy    | Statut ON/OFF                     |

OPTIONS ACTIVEES
| Option      | Description                       |
|-------------|-----------------------------------|
| Boost       | Boost performance temporaire      |
| SSL         | Certificat SSL actif              |
| CDN         | CDN avec nb domaines              |
| IP dediee   | Site independant                  |
| CloudDb     | Serveurs BDD attaches             |

INTERACTIONS
- Clic Modifier (PHP) → Onglet OvhConfig
- Clic Gerer (Runtimes) → Onglet Runtimes
- Clic Gerer (Variables) → Onglet Variables
- Clic Gerer (Cron) → Onglet Cron
- Clic &#128203; → Copier IP dans presse-papier
- Clic carte option → Onglet correspondant

API ENDPOINTS
GET /hosting/web/{serviceName}                     → Infos generales
GET /hosting/web/{sn}/runtime                      → Liste runtimes
GET /hosting/web/{sn}/envVar                       → Liste variables
GET /hosting/web/{sn}/cron                         → Liste cron jobs
GET /hosting/web/{sn}/attachedDomain               → Options CDN/IP
GET /hosting/web/{sn}/serviceInfos                 → Infos service

================================================================================
