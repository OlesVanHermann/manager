================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.expert.emails.svg
Route:   /web-cloud/hosting/{serviceName}/expert/emails
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| monsite.example.com      | GET /hosting/web → serviceName                   |
| ssl0.ovh.net             | GET /hosting/web/{sn} → email.smtp               |
| 465 (SSL)                | GET /hosting/web/{sn} → email.port               |
| 200 / 200                | GET /hosting/web/{sn} → email.quota              |
| 45                       | GET /hosting/web/{sn}/email/stat → sentToday     |
| 2                        | GET /hosting/web/{sn}/email/stat → blockedToday  |
| Bonne                    | GET /hosting/web/{sn}/email/stat → reputation    |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General Sites Offre [Expert]
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: General Boost Indy [Emails] Variables OvhConfig Runtimes Cron +2
   - Contenu:
     - Bloc Configuration SMTP (statut, serveur, port, quota, stats)
     - Banniere warning si emails bloques
     - Bloc Actions (Bloquer, Purger, Reinitialiser)
     - Bloc Historique envois (table 7 jours)

INTERACTIONS
- Clic 📋 -> Copier config SMTP
- Bouton Bloquer envoi -> Bloque SMTP
- Bouton Purger file -> Vide file attente
- Bouton Reinitialiser quota -> Reset quota journalier
- Clic Details -> Modal details email
- Clic Voir details warning -> Liste emails bloques

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement (email config)
GET /hosting/web/{sn}/email/stat           # Statistiques email
GET /hosting/web/{sn}/email/bounces        # Emails bloques/bounces
POST /hosting/web/{sn}/email/flush         # Purger file
POST /hosting/web/{sn}/email/block         # Bloquer/debloquer
POST /hosting/web/{sn}/email/resetQuota    # Reset quota

================================================================================
