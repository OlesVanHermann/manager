================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.sites.ftp.svg
Route:   /web-cloud/hosting/{serviceName}/sites/ftp
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG                        | Source API                                    |
|------------------------------------|-----------------------------------------------|
| monsite.example.com                | GET /hosting/web → serviceName                |
| ftp.cluster123.hosting.ovh.net     | GET /hosting/web/{sn} → ftp.url               |
| 22                                 | GET /hosting/web/{sn} → ftp.portSftp          |
| 21                                 | GET /hosting/web/{sn} → ftp.port              |
| monsite.example.com (user)         | GET /hosting/web/{sn}/user → login            |
| /www                               | GET /hosting/web/{sn}/user/{u} → home         |
| Actif/Inactif                      | GET /hosting/web/{sn}/user/{u} → state        |
| SSH ON/OFF                         | GET /hosting/web/{sn}/user/{u} → sshState     |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Hebergement actif)
3. LEFT PANEL (280px)
   - NAV3 Groupement: General [Sites] Offre Expert
   - Recherche + Liste services
4. RIGHT PANEL (1096px)
   - Header: Titre service
   - NAV4: Multisite SSL [FTP-SSH] BDD CDN
   - Contenu:
     - Bloc Informations connexion FTP (serveur, ports)
     - Toolbar: Refresh, Recherche, + Creer utilisateur
     - Table utilisateurs: Login, Repertoire, Statut, SSH, Date, Actions
     - Pagination
     - Bloc Acces SSH (statut global, commande)

INTERACTIONS
- Bouton + Creer utilisateur FTP -> Modal creation utilisateur
- Clic 🔑 -> Modal changer mot de passe
- Clic ✎ -> Modal modifier utilisateur
- Clic × -> Modal confirmation suppression
- Clic SSH ON/OFF -> Toggle acces SSH utilisateur
- Bouton Desactiver SSH -> Desactive SSH global

API ENDPOINTS
GET /hosting/web/{sn}                      # Details hebergement (FTP info)
GET /hosting/web/{sn}/user                 # Liste utilisateurs FTP
GET /hosting/web/{sn}/user/{login}         # Details utilisateur
POST /hosting/web/{sn}/user                # Creer utilisateur
PUT /hosting/web/{sn}/user/{login}         # Modifier utilisateur
DELETE /hosting/web/{sn}/user/{login}      # Supprimer utilisateur
POST /hosting/web/{sn}/user/{login}/changePassword # Changer mot de passe

================================================================================
