================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.dns-zone.svg
Route:   /web-cloud/domaines/zone/{zoneName}
NAV:     Web Cloud > Domaines et DNS > Zone DNS
Version: v1 (2025-12-29)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                      |
|--------------------------|-------------------------------------------------|
| example.com              | GET /domain/zone/{z} -> zoneName                |
| 213.186.33.5             | GET /domain/zone/{z}/record -> target           |
| 12 entrees               | GET /domain/zone/{z}/record -> count            |
================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Domaines et DNS actif)
3. LEFT PANEL - Liste zones DNS
4. RIGHT PANEL
   - Title: Zone DNS de {zoneName}
   - Toolbar: Ajouter, Importer, Exporter, Reinitialiser, Filtre
   - Table enregistrements DNS
   - Pagination

NOTE: Cette page affiche une zone DNS independante (sans domaine associe).
Pour les zones liees a un domaine, voir target_.web-cloud.domain.dns.svg

TOOLBAR ACTIONS
| Bouton              | Style    | Action                    |
|---------------------|----------|---------------------------|
| + Ajouter une entree| Primaire | Modal ajout enregistrement|
| Importer            | Outline  | Modal import zone         |
| Exporter            | Outline  | Telecharger fichier zone  |
| Reinitialiser       | Outline  | Modal confirmation reset  |

TABLE COLONNES
| Colonne     | Description                          |
|-------------|--------------------------------------|
| Sous-domaine| @ ou nom du sous-domaine             |
| Type        | Badge colore (A, CNAME, MX, TXT...)  |
| TTL         | Duree en secondes                    |
| Cible       | Valeur de l'enregistrement           |
| Actions     | Menu contextuel (⋮)                  |

API ENDPOINTS
GET    /domain/zone/{zoneName}                   -> Info zone
GET    /domain/zone/{zoneName}/record            -> Liste enregistrements
POST   /domain/zone/{zoneName}/record            -> Ajouter enregistrement
PUT    /domain/zone/{zoneName}/record/{id}       -> Modifier enregistrement
DELETE /domain/zone/{zoneName}/record/{id}       -> Supprimer enregistrement
================================================================================
