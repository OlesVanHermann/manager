================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domain.modal-batch-dns.svg
Route:   /web-cloud/domaines (modal batch depuis index)
NAV:     Modal accessible depuis la liste des domaines (index)
Version: v1 (2025-12-29)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                              |
|--------------------------|---------------------------------------------------------|
| example.com              | Domaines sélectionnés par l'utilisateur                |
| dns1.ovh.net, ns1.ovh.net| GET /domain/{d}/nameServer -> host                     |
================================================================================

STRUCTURE MODAL
1. Header: Titre + bouton fermer
2. Bannière explicative
3. Formulaire serveurs DNS:
   - DNS 1 (obligatoire)
   - DNS 2 (obligatoire)
   - DNS 3 (optionnel)
   - DNS 4 (optionnel)
4. Table domaines sélectionnés:
   - Checkbox
   - Domaine
   - DNS actuels
5. Avertissement (propagation 48h)
6. Footer: Annuler | Appliquer (N)

VALIDATION
| Champ   | Règle                                    |
|---------|------------------------------------------|
| DNS 1   | Obligatoire, FQDN valide                 |
| DNS 2   | Obligatoire, FQDN valide                 |
| DNS 3   | Optionnel, FQDN valide si renseigné      |
| DNS 4   | Optionnel, FQDN valide si renseigné      |

SERVEURS DNS OVH
| Serveur      | Adresse IP     |
|--------------|----------------|
| dns1.ovh.net | 213.186.33.99  |
| ns1.ovh.net  | 213.186.33.99  |
| dns2.ovh.net | 213.186.33.100 |
| ns2.ovh.net  | 213.186.33.100 |

API ENDPOINTS
PUT /domain/{domain}/nameServer
Body: {
  "nameServers": [
    { "host": "dns1.ovh.net" },
    { "host": "ns1.ovh.net" }
  ]
}

Note: L'API est appelée pour chaque domaine sélectionné

ACTIONS
- Click checkbox -> Toggle sélection domaine
- Edit champs DNS -> Modifier les serveurs
- Click "Annuler" -> Fermer modal
- Click "Appliquer" -> Exécuter l'opération batch
================================================================================
