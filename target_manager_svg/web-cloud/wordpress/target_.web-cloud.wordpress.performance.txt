================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.wordpress.performance.svg
Route:   /web-cloud/wordpress/{serviceName}/performance
Version: v1 (2025-12-30)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                                    |
|--------------------------|-----------------------------------------------|
| Active (CDN)             | GET /managedCMS/resource/{sn}/cdn → enabled   |
| OVHcloud CDN             | GET /managedCMS/resource/{sn}/cdn → provider  |
| 19 PoP                   | GET /managedCMS/resource/{sn}/cdn → popCount  |
| 12.4 Go                  | GET /managedCMS/resource/{sn}/cdn → bandwidth |
| Actif (cache)            | GET /managedCMS/resource/{sn}/cache → enabled |
| 29/12/2025 14:32         | GET /managedCMS/resource/{sn}/cache → lastFlush |
================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif x=510)
2. NAV2 - Web Cloud (Wordpress actif x=345)
3. LEFT PANEL - Liste des sites WordPress
4. RIGHT PANEL
   - Header: Nom site + version + offre
   - NAV3: 6 tabs (Performances actif)
   - Contenu Performances:
     * Bloc CDN (505px):
       - Statut (Active/Inactive)
       - Fournisseur
       - Points de presence
       - Bande passante
       - Boutons: Configurer / Desactiver
     * Bloc CACHE (516px):
       - Cache serveur (statut)
       - Cache navigateur (statut)
       - Derniere purge (date)
       - Bouton: Vider le cache
     * Bloc OPTIMISATIONS (full width):
       - GZIP, Brotli, HTTP/2, HTTP/3
       - Recommandations plugins (WebP, Lazy Loading)
     * Banniere conseil

ETATS CDN
- CDN active: cercle vert + "Active"
- CDN inactive: cercle gris + "Inactive"
- CDN non disponible (Start): message "Disponible avec offre Pro/Business"

ETATS OPTIMISATIONS
- Active: cercle vert
- Plugin recommande: cercle orange (non inclus nativement)

INTERACTIONS
- Clic "Configurer CDN" → Modal configuration CDN
- Clic "Desactiver" → POST /cdn/disable
- Clic "Vider le cache" → POST /cache/flush

DISPONIBILITE PAR OFFRE
| Feature | Start | Pro | Business |
|---------|-------|-----|----------|
| Cache serveur | OK | OK | OK |
| CDN | - | OK | OK |
| HTTP/3 | - | OK | OK |

API ENDPOINTS
GET  /managedCMS/resource/{sn}/cdn           - Etat CDN
POST /managedCMS/resource/{sn}/cdn/enable    - Activer CDN
POST /managedCMS/resource/{sn}/cdn/disable   - Desactiver CDN
GET  /managedCMS/resource/{sn}/cache         - Etat cache
POST /managedCMS/resource/{sn}/cache/flush   - Vider cache

================================================================================
