================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.emails.index.svg
Route:   /web-cloud/emails
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                              |
|-----------------------|-----------------------------------------|
| example.com           | GET /email/domains → domain             |
| 12 boîtes             | GET /email/domains/{d}/summary → count  |
| 67 Go                 | GET /email/domains/{d}/summary → quota  |
| 5 Exchange            | GET /email/domains/{d}/summary → offers |
| ceo@example.com       | GET /email/domains/{d}/accounts → email |
| Pack Biz 10           | GET /email/licenses/packs/{id} → name   |
| ab12345-ovh           | Session → nicHandle                     |

================================================================================

# STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif, x=510)
2. NAV2 - Web Cloud (Emails actif, x=430)
3. LEFT PANEL (280px)
   - Toggle vue: Par domaine / Par licence
   - Recherche domaine
   - Filtre par offre
   - Liste domaines avec breakdown offres
   - Bouton "+ Ajouter domaine"
4. RIGHT PANEL (1096px)
   - Header: Nom domaine + compteurs + Webmail
   - NAV3: 8 tabs (Boîtes mail, Redirections, Répondeurs, Listes, Sécurité, Avancé, Licences, Tâches)
   - Toolbar: Créer boîte, Importer, Exporter, Recherche, Refresh
   - Filtres offre (badges colorés)
   - Table comptes avec colonnes: Email, Offre, Licence/Pack, Quota, Statut, Actions
   - Pagination

# INTERACTIONS

- Toggle vue → Bascule entre vue domaine et vue licence
- Clic domaine left panel → Sélection + chargement right panel
- Clic tab NAV3 → Navigation vers section
- Clic "+ Créer boîte ▾" → Modal création avec choix licence
- Clic filtre offre → Filtre table
- Actions ligne:
  - ✎ → Modal modifier compte
  - 🔑 → Modal changer mot de passe
  - 📧 → Modal gérer alias (Pro/Exchange/Zimbra)
  - 📤 → Modal délégation (Pro/Exchange)
  - 🔐 → Modal MFA (Exchange only)
  - ⋯ → Menu plus d'actions

# BADGES OFFRES

| Offre    | Couleur badge | Couleur dot |
|----------|---------------|-------------|
| Exchange | #FEF3C7       | #F59E0B     |
| Pro      | #DBEAFE       | #3B82F6     |
| Zimbra   | #EDE9FE       | #8B5CF6     |
| MX Plan  | #F3F4F6       | #6B7280     |

# API ENDPOINTS

GET  /email/domains                       → Liste domaines email
GET  /email/domains/{domain}              → Détails domaine
GET  /email/domains/{domain}/summary      → Résumé (counts, quotas)
GET  /email/domains/{domain}/accounts     → Liste comptes
POST /email/domains/{domain}/accounts     → Créer compte
POST /email/domains                       → Ajouter domaine

================================================================================
