================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.access.pack-xdsl.modem-firmware.svg
Route:   /web-cloud/access/connections/{connectionId}/modem-firmware
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| v3.2.1                | GET /access/connections/{id}/modem/firmware → current |
| TG799vac Xtream       | GET /access/connections/{id}/modem → model    |
| Liste versions        | GET /access/connections/{id}/modem/firmware/available |

================================================================================

# STRUCTURE PAGE
1. NAV4: [Dashboard] | WiFi | DHCP | NAT/Ports | Routeur
2. Bloc VERSION ACTUELLE: Version, date, modèle, badge statut
3. Bloc CHANGER DE VERSION: Dropdown + bouton appliquer
4. Bloc WARNING: Informations redémarrage
5. Bloc HISTORIQUE: Tableau versions précédentes

# INTERACTIONS
- Dropdown version → Sélection version disponible
- Clic "Appliquer" → Confirmation + POST mise à jour
- Pendant mise à jour → Progress indicator + message

# API ENDPOINTS
GET  /access/connections/{id}/modem/firmware           - Version actuelle
GET  /access/connections/{id}/modem/firmware/available - Versions disponibles
POST /access/connections/{id}/modem/firmware           - Lancer mise à jour

================================================================================
