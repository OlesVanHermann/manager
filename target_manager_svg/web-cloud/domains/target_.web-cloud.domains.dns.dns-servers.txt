================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.domains.dns.dns-servers.svg
Route:   /web-cloud/domains/{domainName}/dns-servers
Version: v1 (2026-01-02)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG           | Source API                              |
|-----------------------|-----------------------------------------|
| exemple.com           | GET /domain/{domain} -> name            |
| OVH (heberges)        | GET /domain/{domain} -> nameServerType  |
| dns111.ovh.net        | GET /domain/{domain}/nameServer/{id} -> host |
| OK                    | GET /domain/{domain}/nameServer/{id}/status |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Domaines et DNS actif
3. LEFT PANEL (280px)
   - NAV3 Groupement: [General] [DNS*] [Expert]
   - Recherche + liste services
4. RIGHT PANEL (1096px)
   - Header: {domainName}
   - NAV4 Tabs: 12 tabs DNS dont [Serveurs DNS*] actif
   - Card configuration DNS
   - Toolbar: Refresh, Modifier les DNS
   - Table serveurs: Host, IP, Statut, Actions
   - Banniere info Anycast

# INTERACTIONS
- Clic [Modifier les DNS] -> DnsChangeModal
- Clic [Supprimer] -> Supprime nameserver
- Clic [En savoir plus] -> Navigate /anycast

# API ENDPOINTS
GET /domain/{domain}
GET /domain/{domain}/nameServer (retourne number[] ou Iceberg)
GET /domain/{domain}/nameServer/{id}
GET /domain/{domain}/nameServer/{id}/status
POST /domain/{domain}/nameServer
DELETE /domain/{domain}/nameServer/{id}
POST /domain/{domain}/nameServers/update

# MODALS
- DnsChangeModal: Modifier les serveurs DNS (hosted/external)

================================================================================
