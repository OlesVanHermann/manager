================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.voip.line.filtering.svg
Route:   /web-cloud/voip/{billingAccount}/line/{serviceName}/calls/filtering
Version: v1 (2024-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG               | Source API                                               |
|---------------------------|----------------------------------------------------------|
| 0033899*                  | GET /telephony/{ba}/line/{sn}/calls/filtering → pattern |
| Bloquer/Autoriser         | GET /telephony/{ba}/line/{sn}/calls/filtering → type    |
| Raccrocher                | GET /telephony/{ba}/line/{sn}/calls/filtering → action  |
| Description               | GET /telephony/{ba}/line/{sn}/calls/filtering → name    |

================================================================================

# STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - VoIP actif
3. LEFT PANEL - Liste des lignes du groupe
4. RIGHT PANEL
   - Header: Numéro + description
   - NAV3: Tabs (Appels actif)
   - NAV4: Sous-tabs (Filtrage entrant actif)
   - Toolbar + Table règles

# TABLE RÈGLES
| Colonne | Source |
|---------|--------|
| Type | rule.type (Bloquer/Autoriser) |
| Numéro/Préfixe | rule.pattern |
| Action | rule.action |
| Description | rule.name |
| Actions | Modifier, Supprimer |

# TYPES DE RÈGLES
- Bloquer (badge rouge) → Rejette l'appel
- Autoriser (badge vert) → Accepte l'appel

# ACTIONS POSSIBLES
- Raccrocher → Coupe l'appel
- Messagerie → Redirige vers messagerie
- Sonner → Laisse sonner normalement

# INTERACTIONS
- "+ Ajouter une règle" → Modal création
- "✎ Modifier" → Modal édition
- "× Supprimer" → Confirmation suppression

# API ENDPOINTS
GET /telephony/{ba}/line/{sn}/calls/filtering
POST /telephony/{ba}/line/{sn}/calls/filtering
PUT /telephony/{ba}/line/{sn}/calls/filtering/{id}
DELETE /telephony/{ba}/line/{sn}/calls/filtering/{id}

================================================================================
