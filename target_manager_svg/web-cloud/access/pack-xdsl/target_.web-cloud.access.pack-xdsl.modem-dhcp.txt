================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.access.pack-xdsl.modem-dhcp.svg
Route:   /web-cloud/access/connections/{connectionId}/modem-dhcp
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| Plage DHCP            | GET /access/connections/{id}/modem/dhcp → range |
| Baux actifs           | GET /access/connections/{id}/modem/dhcp/leases |
| Réservations          | GET /access/connections/{id}/modem/dhcp/reservations |

================================================================================

# STRUCTURE PAGE
1. NAV4: Dashboard | WiFi | [DHCP] | NAT/Ports | Routeur
2. Bloc SERVEUR DHCP: Toggle + config (plage, DNS)
3. Bloc BAUX ACTIFS: Table des appareils connectés
4. Bloc RÉSERVATIONS: Liste + bouton ajouter

# INTERACTIONS
- Toggle DHCP → Active/désactive serveur
- Clic "Modifier" config → Modal config DHCP
- Clic "📌 Réserver" → Crée réservation pour appareil
- Clic "Ajouter" réservation → Modal nouvelle réservation
- Clic ✎ → Modifier réservation
- Clic × → Supprimer réservation

# API ENDPOINTS
GET  /access/connections/{id}/modem/dhcp              - Config DHCP
PUT  /access/connections/{id}/modem/dhcp              - Modifier config
GET  /access/connections/{id}/modem/dhcp/leases       - Baux actifs
GET  /access/connections/{id}/modem/dhcp/reservations - Réservations
POST /access/connections/{id}/modem/dhcp/reservations - Ajouter

================================================================================
