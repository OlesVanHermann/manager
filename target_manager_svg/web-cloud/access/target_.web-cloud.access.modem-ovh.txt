================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.access.modem-ovh.svg
Route:   /web-cloud/access/connections/{id}/modem
Version: v1 (2025-12-30)
================================================================================

⚠️ DONNÉES DYNAMIQUES
| Exemple SVG              | Source API                                       |
|--------------------------|--------------------------------------------------|
| Livebox 6 Pro            | GET /modem → model                               |
| 4.12.0                   | → firmware.version                               |
| ✓ À jour                 | → firmware.upToDate                              |
| 127j 08:45:12            | → uptime                                         |
| MonReseau                | → wifi[0].ssid                                   |
| WPA3                     | → wifi[0].security                               |
| Auto (6)                 | → wifi[0].channel                                |
| 12                       | → wifi[0].connectedDevices                       |
| 192.168.1.1              | → lan.ipAddress                                  |
| 20 appareils             | → dhcp.activeLeases                              |
| Routeur                  | → mode                                           |
| 3 règles                 | → nat.rulesCount                                 |

================================================================================

# STRUCTURE PAGE

1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Section (Accès actif)
3. LEFT PANEL - Liste connexions
4. RIGHT PANEL
   - Header: Nom connexion
   - NAV3: 7 tabs (Modem actif)
   - NAV4: 5 sous-tabs (Dashboard actif)
     - Dashboard (défaut)
     - WiFi
     - DHCP
     - NAT/Ports
     - Routeur
   - Contenu Dashboard:
     - Header modem (modèle, badge OVH, firmware, uptime, actions)
     - WiFi 2.4 GHz (SSID, sécurité, canal, appareils, actions)
     - WiFi 5 GHz (idem)
     - RÉSEAU LOCAL (IP, plage DHCP, baux actifs)
     - MODE (routeur/bridge, DMZ, firewall)
     - NAT summary (règles, UPnP)

# ACTIONS DISPONIBLES
- 🔄 Redémarrer → POST /modem/reboot
- ⬆ Mise à jour → POST /modem/update
- ⚙ Configurer WiFi → Modal
- 🔑 Mot de passe → Modal
- ⚙ DHCP → Navigation
- 📋 Baux → Modal liste
- ⚙ Changer mode → Modal

# API ENDPOINTS
GET  /access/connections/{id}/modem         - Infos modem
POST /access/connections/{id}/modem/reboot  - Redémarrer
POST /access/connections/{id}/modem/update  - Mise à jour

================================================================================
