# Cahier des Charges : web-cloud/access/pack-xdsl - IP

## 1. VUE D'ENSEMBLE
- **Écran** : Gestion des IP d'un accès xDSL
- **Objectif** : Configurer les adresses IP et reverse DNS du bloc
- **Contexte** : Administration réseau avancée

## 2. LAYOUT & STRUCTURE
- Carte blanche avec titre incluant le bloc IP
- Tableau des adresses IP
- Bandeau info avec statistiques d'utilisation

## 3. COMPOSANTS UI
- **OdsCard** : Container principal
- **OdsTable** : Liste des IP
- **OdsInput** : Champs édition IP et reverse DNS
- **OdsButton** : Supprimer (danger), Valider, Annuler, Ajouter (+)
- **OdsMessage** : Bandeau info bleu

## 4. DONNÉES & COLONNES
| Colonne | Type | Description |
|---------|------|-------------|
| Adresse IP | text | 91.134.xxx.X |
| Reverse DNS | text/input | FQDN ou "(non configuré)" |
| Actions | buttons | Supprimer (🗑) |

## 5. ÉTATS DE LIGNE
- **Normal** : Affichage lecture seule
- **Édition** : Fond bleu clair, champs modifiables, boutons ✓/✕
- **Ajout** : Ligne vide avec bouton +

## 6. BANDEAU INFO
- Texte : "Vous disposez de 8 adresses IP dans ce bloc"
- Statistiques : "4 sont utilisées, 4 sont disponibles"

## 7. ACTIONS & INTERACTIONS
- Clic ligne → Mode édition
- Bouton ✓ → Sauvegarder modification
- Bouton ✕ → Annuler modification
- Bouton 🗑 → Supprimer reverse DNS
- Bouton + → Ajouter nouvelle IP

## 8. APIS & ENDPOINTS
- GET /xdsl/{serviceName}/ips : Liste IP
- PUT /xdsl/{serviceName}/ips/{ip}/reverse : Modifier reverse DNS
- DELETE /xdsl/{serviceName}/ips/{ip}/reverse : Supprimer reverse
