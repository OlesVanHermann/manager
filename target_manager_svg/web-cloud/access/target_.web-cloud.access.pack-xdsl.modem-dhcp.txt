# Cahier des Charges : web-cloud/access/pack-xdsl - Modem DHCP

## 1. VUE D'ENSEMBLE
- **Écran** : Configuration DHCP du modem
- **Objectif** : Gérer le serveur DHCP et les baux statiques
- **Contexte** : Configuration réseau avancée

## 2. LAYOUT & STRUCTURE
- Carte principale avec titre DHCP
- Tableau config DHCP (éditable inline)
- Section baux statiques DHCP

## 3. COMPOSANTS UI
- **OdsCard** : Container principal
- **OdsTable** : Config DHCP + Baux statiques
- **OdsCheckbox** : Serveur DHCP actif
- **OdsInput** : Champs configuration
- **OdsBadge** : État serveur (Actif)
- **OdsButton** : ✎ éditer, ✓ valider, ✕ annuler, 🗑 supprimer

## 4. TABLEAU CONFIG DHCP
| Colonne | Description |
|---------|-------------|
| Serveur DHCP | Badge Actif + checkbox en édition |
| Passerelle ⓘ | IP gateway (192.168.1.254) |
| DNS primaire ⓘ | IP DNS 1 (213.186.33.99) |
| DNS secondaire ⓘ | IP DNS 2 (optionnel) |
| Domaine DNS ⓘ | Nom domaine local (lan) |
| Plage DHCP ⓘ | IP début + IP fin |
| Bail (s) ⓘ | Durée en secondes (86400) |
| Actions | Boutons édition |

## 5. ÉTATS DE LIGNE
- **Lecture** : Valeurs affichées, bouton ✎
- **Édition** : Champs input, fond bleu clair, boutons ✓/✕

## 6. SECTION BAUX STATIQUES
### Colonnes
| Colonne | Description |
|---------|-------------|
| Adresse MAC | MAC de l'appareil |
| Adresse IP | IP fixe attribuée |
| Nom | Nom descriptif |
| Actions | ✎ éditer, 🗑 supprimer |

### Exemple
- AA:BB:CC:DD:EE:FF → 192.168.1.100 (serveur-nas)

## 7. APIS & ENDPOINTS
- GET /xdsl/{serviceName}/modem/dhcp : Config DHCP
- PUT /xdsl/{serviceName}/modem/dhcp : Modifier config
- GET /xdsl/{serviceName}/modem/dhcp/staticLeases : Baux statiques
- POST /xdsl/{serviceName}/modem/dhcp/staticLeases : Ajouter bail
- DELETE /xdsl/{serviceName}/modem/dhcp/staticLeases/{mac} : Supprimer
