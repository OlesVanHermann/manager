================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.modal.add-domain.svg
Route:   /web-cloud/hosting/{serviceName}/multisite (modal)
Version: v1 (2025-12-29)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG              | Source API                              |
|--------------------------|-----------------------------------------|
| www.exemple.com          | Input utilisateur                       |
| www/exemple              | Input utilisateur (auto-généré)         |
| PHP 8.2                  | GET /hosting/web/{sn}/availablePhp      |
================================================================================

# STRUCTURE MODAL
1. HEADER - Titre "Ajouter un domaine" + close
2. FORMULAIRE:
   - Nom de domaine (input text) *
   - Dossier racine (input text) *
   - SSL Let's Encrypt (checkbox, coché par défaut)
   - Firewall ModSecurity (checkbox, coché par défaut)
   - CDN (checkbox, décoché - option payante)
   - Version PHP (select)
3. ACTIONS: Annuler, Ajouter

# VALIDATION
- Domaine: format DNS valide, pas déjà attaché
- Dossier: format chemin valide, pas de caractères spéciaux

# API ENDPOINTS
POST /hosting/web/{serviceName}/attachedDomain - Ajouter domaine
================================================================================
