================================================================================
TARGET SVG DOCUMENTATION
================================================================================
Fichier: target_.web-cloud.hosting.cdn-settings.svg
Route:   /web-cloud/hebergement/hosting/{serviceName}/cdn/{domain}/settings
Version: v1 (2025-12-30)
================================================================================

DONNEES DYNAMIQUES
| Exemple SVG           | Source API                                    |
|-----------------------|-----------------------------------------------|
| www.example.com       | GET /hosting/web/{sn}/cdn/{domain}            |
| Active                | GET /hosting/web/{sn}/cdn/{domain} -> http2   |
| Active                | GET /hosting/web/{sn}/cdn/{domain} -> brotli  |
| Desactive             | GET /hosting/web/{sn}/cdn/{domain} -> geoLoc  |
| Active                | GET /hosting/web/{sn}/cdn/{domain} -> hsts    |
| static-assets         | GET /hosting/web/{sn}/cdn/{domain}/rules      |

================================================================================

STRUCTURE PAGE
1. NAV1 - Header OVH (Web Cloud actif)
2. NAV2 - Hebergement actif
3. LEFT PANEL - Liste hebergements
4. RIGHT PANEL
   - Header: Parametres CDN - {domain}
   - INFO BANNER: Propagation 5 min
   - PERFORMANCE OPTIONS: Always Online, HTTP/2, Brotli, Geo Headers
   - SECURITY OPTIONS: HSTS, Mixed Content
   - CACHE RULES TABLE: Nom, Type, Pattern, TTL, Actions

API ENDPOINTS
GET    /hosting/web/{sn}/cdn/{domain}                Info domaine CDN
PUT    /hosting/web/{sn}/cdn/{domain}                Modifier options
GET    /hosting/web/{sn}/cdn/{domain}/rules          Liste regles cache
POST   /hosting/web/{sn}/cdn/{domain}/rules          Ajouter regle
PUT    /hosting/web/{sn}/cdn/{domain}/rules/{id}     Modifier regle
DELETE /hosting/web/{sn}/cdn/{domain}/rules/{id}     Supprimer regle

================================================================================
