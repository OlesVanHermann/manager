# Cahier des Charges : iam/components/delete-entity

## 1. VUE D'ENSEMBLE

| Champ | Valeur |
|-------|--------|
| **Fichier SVG** | old_manager.iam.components.delete-entity.svg |
| **NAV1** | iam |
| **NAV2** | policies |
| **Type** | Modal warning |
| **Description** | Modal de suppression d'une entité IAM (politique) avec confirmation "DELETE" |

---

## 2. MODAL

### 2.1 Dimensions

| Propriété | Valeur |
|-----------|--------|
| Largeur | 600px |
| Hauteur | 380px |
| Position | Centrée (x: 420, y: 200) |
| Border radius | 8px |
| Shadow | drop-shadow(0 4px 20px rgba(0,0,0,0.15)) |

### 2.2 Overlay

| Propriété | Valeur |
|-----------|--------|
| Background | rgba(0, 0, 0, 0.5) |

---

## 3. HEADER MODAL

| Propriété | Valeur |
|-----------|--------|
| Hauteur | 60px |
| Background | #FEF3C7 (warning light) |
| Titre | Suppression d'une politique |
| Couleur titre | #D97706 |
| Font-size | 18px |
| Font-weight | 600 |
| Bouton fermer | × (warning) |

---

## 4. CONTENU

### 4.1 Message d'avertissement

| Texte ligne 1 | La suppression d'une politique entraine la perte de droits d'accès |
| Texte ligne 2 | des identités sur les ressources. |
| Font-size | 13px |

### 4.2 Nom de l'entité

| Label | Suppression de la politique : |
| Valeur | ma-politique-production |
| Font-weight | 600 |

### 4.3 Champ de confirmation

| Label | Veuillez écrire le mot "DELETE" pour supprimer votre politique |
| Type | Input text |
| Dimensions | 552×36px |
| Placeholder | - |
| Curseur | Visible |

### 4.4 Avertissement irréversible

| Dimensions | 552×40px |
| Background | #FEE2E2 |
| Texte | Cette action est irréversible. |
| Couleur texte | #DC2626 |
| Font-size | 11px |

---

## 5. FOOTER MODAL

| Bouton | Style | Dimensions | État |
|--------|-------|------------|------|
| Annuler | Outline | 100×36px | Actif |
| Supprimer | Primary disabled | 106×36px | Disabled (tant que DELETE pas tapé) |

---

## 6. VALIDATION

| Règle | Valeur |
|-------|--------|
| Texte requis | DELETE (exact, case-sensitive) |
| Bouton actif | Seulement quand input = "DELETE" |

---

## 7. APIS & ENDPOINTS

### 7.1 Supprimer une politique

```
DELETE /me/iam/policy/{policyId}
```

---

## 8. TRADUCTIONS (i18n)

| Clé | Valeur FR |
|-----|-----------|
| `iam_delete_entity_title` | Suppression d'une politique |
| `iam_delete_entity_warning` | La suppression d'une politique entraine la perte de droits d'accès des identités sur les ressources. |
| `iam_delete_entity_name` | Suppression de la politique : |
| `iam_delete_entity_confirm_label` | Veuillez écrire le mot "DELETE" pour supprimer votre politique |
| `iam_delete_entity_irreversible` | Cette action est irréversible. |
| `iam_delete_entity_cancel` | Annuler |
| `iam_delete_entity_submit` | Supprimer |
