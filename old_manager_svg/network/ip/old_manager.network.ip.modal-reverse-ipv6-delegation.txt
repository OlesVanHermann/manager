# Cahier des Charges : network/ip - Modal Reverse IPv6 Delegation

## 1. VUE D'ENSEMBLE

**Type:** Modal wizard 2 étapes
**Fonction:** Gérer la délégation reverse DNS IPv6
**Contexte:** Action depuis la gestion des blocs IPv6

## 2. DIMENSIONS

- Modal: 550x450px
- Border radius: 8px

## 3. HEADER MODAL

- Background: #000E9C
- Titre: "Délégation reverse DNS IPv6" (15px, blanc, bold)
- Bouton fermer: × (18px, blanc)

## 4. STEPPER

| Étape | Nom | État |
|-------|-----|------|
| 1 | Configuration | Actif (#0050D7) |
| 2 | Confirmation | Inactif |

## 5. MESSAGE INFO

- Container: 500x35px
- Background: #EFF6FF
- Border: 1px solid #BFDBFE
- Icon: Cercle bleu (i)
- Texte: "Vous pouvez ajouter jusqu'à 2 serveurs de noms pour la délégation."

## 6. CONTENU

### Section serveurs DNS actuels
- Titre: "Serveurs DNS actuels" (13px, bold)

| Serveur | Actions |
|---------|---------|
| ns1.example.com | 🗑 (supprimer) |
| ns2.example.com | 🗑 (supprimer) |

### Section ajouter serveur
- Titre: "Ajouter un serveur DNS" (13px, bold)
- Input: 400x36px
- Placeholder: "ns3.example.com"
- Bouton: "Ajouter" (90x36px, btn-outline)

## 7. FOOTER MODAL

| Bouton | Style | Dimensions |
|--------|-------|------------|
| Annuler | btn-outline | 100x36px |
| Suivant | btn-primary | 100x36px |

## 8. APIS & ENDPOINTS

```
# Liste délégations reverse IPv6
GET /ip/{ip}/reverse
→ [{
  ipReverse: "2001:41d0:302:2100::/56",
  ns: "ns1.example.com"
}, ...]

# Ajouter délégation
POST /ip/{ip}/reverse
Body: {
  ns: "ns3.example.com"
}

# Supprimer délégation
DELETE /ip/{ip}/reverse/{ns}
```

## 9. TRADUCTIONS (i18n)

| Clé | FR |
|-----|-----|
| modal_title | Délégation reverse DNS IPv6 |
| step_config | Configuration |
| step_confirm | Confirmation |
| info_max_ns | Vous pouvez ajouter jusqu'à 2 serveurs de noms pour la délégation |
| current_dns | Serveurs DNS actuels |
| add_dns | Ajouter un serveur DNS |
| btn_add | Ajouter |
| btn_cancel | Annuler |
| btn_next | Suivant |
