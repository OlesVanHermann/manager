# Cahier des Charges : network/load-balancer - Listing Farms

## 1. VUE D'ENSEMBLE

**Type:** Page listing avec accordéons
**Fonction:** Lister et gérer les fermes de serveurs du load balancer
**Contexte:** Configuration load balancer

## 2. LAYOUT

- Page: 1100x600px
- Background: #F9FAFB
- Header OVHcloud: 56px, #000E9C

## 3. HEADER PAGE

| Élément | Style |
|---------|-------|
| Titre | "Fermes de serveurs" (18px, font-weight: 600) |
| Sous-titre | "Gérez vos fermes de serveurs et leurs backends" (text-muted, 13px) |

## 4. ACTION BAR

| Bouton | Style |
|--------|-------|
| Ajouter une ferme | Secondary (border) |

## 5. ACCORDÉONS FERMES

### Accordéon fermé
| Élément | Style |
|---------|-------|
| Height | 50px |
| Background | #FFFFFF |
| Border | #E5E7EB |
| Border-radius | 8px |
| Chevron | ▶ (fermé) |
| Label | "{farm-name} ({id})" (font-weight: 500) |
| Info | "{protocol} - Port {port} - {datacenter}" (text-muted, 12px) |
| ActionMenu | ⋮ |

### Accordéon ouvert
| Élément | Style |
|---------|-------|
| Header | 50px, background #F3F4F6, border-radius top |
| Chevron | ▼ (ouvert) |
| Content | background #FFFFFF |

### Bouton dans accordéon ouvert
| Bouton | Style |
|--------|-------|
| Ajouter un serveur | Secondary (border), 32px height |

## 6. DATAGRID SERVEURS (dans accordéon)

### Conteneur
- Width: 980px
- Background: #FFFFFF
- Border: #E5E7EB
- Border-radius: 4px

### Header
- Background: #F3F4F6
- Height: 35px

### Colonnes
| Colonne | Largeur | Type |
|---------|---------|------|
| Serveur | 200px | Texte |
| IPv4 | 200px | Texte |
| Actif | 150px | Toggle switch |
| Statut | 330px | Badge (UP/DOWN) |
| Actions | 100px | Menu ⋮ |

### Toggle switch
| État | Style |
|------|-------|
| Active | #0050D7, cercle blanc à droite |
| Inactive | #E5E7EB, cercle blanc à gauche |

### Badge statut
| État | Background | Text |
|------|------------|------|
| UP | #DCFCE7 | #16A34A |
| DOWN | #FEE2E2 | #DC2626 |

## 7. APIS & ENDPOINTS

```
GET /ipLoadbalancing/{serviceName}/http/farm
GET /ipLoadbalancing/{serviceName}/tcp/farm
GET /ipLoadbalancing/{serviceName}/http/farm/{farmId}
GET /ipLoadbalancing/{serviceName}/http/farm/{farmId}/server
POST /ipLoadbalancing/{serviceName}/http/farm
POST /ipLoadbalancing/{serviceName}/http/farm/{farmId}/server
DELETE /ipLoadbalancing/{serviceName}/http/farm/{farmId}
```

## 8. TRADUCTIONS (i18n)

```json
{
  "server_farms": "Fermes de serveurs",
  "manage_farms_description": "Gérez vos fermes de serveurs et leurs backends",
  "add_farm": "Ajouter une ferme",
  "add_server": "Ajouter un serveur",
  "server": "Serveur",
  "ipv4": "IPv4",
  "active": "Actif",
  "status": "Statut",
  "up": "UP",
  "down": "DOWN",
  "http": "HTTP",
  "https": "HTTPS",
  "tcp": "TCP"
}
```
