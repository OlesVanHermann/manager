# Cahier des Charges : network/load-balancer - Listing Frontends

## 1. VUE D'ENSEMBLE

**Type:** Page listing avec datagrid
**Fonction:** Lister et gérer les frontends du load balancer
**Contexte:** Configuration load balancer

## 2. LAYOUT

- Page: 1100x480px
- Background: #F9FAFB
- Header OVHcloud: 56px, #000E9C

## 3. HEADER PAGE

| Élément | Style |
|---------|-------|
| Titre | "Frontends" (18px, font-weight: 600) |
| Sous-titre | "Gérez les points d'entrée de votre Load Balancer" (text-muted, 13px) |

## 4. ACTION BAR

| Bouton | Style |
|--------|-------|
| Ajouter un frontend | Secondary (border) |

## 5. DATAGRID

### Conteneur
- Width: 1020px
- Height: 270px
- Background: #FFFFFF
- Border: #E5E7EB
- Border-radius: 8px

### Header
- Background: #F3F4F6
- Height: 45px
- Font-size: 13px, font-weight: 500

### Colonnes
| Colonne | Largeur | Type |
|---------|---------|------|
| Frontend | 195px | Link (text-link, nom + ID) |
| Protocole | 160px | Texte ({PROTOCOL}: {port}) |
| Datacenter | 180px | Texte (code + ville) |
| Ferme par défaut | 200px | Texte |
| Actif | 160px | Toggle switch |
| Actions | 125px | Menu ⋮ |

### Toggle switch
| État | Style |
|------|-------|
| ON | #0050D7, cercle blanc à droite |
| OFF | #E5E7EB, cercle blanc à gauche |

### Menu Actions
| Option | Style |
|--------|-------|
| Voir l'aperçu | text-body |
| Modifier | text-body |
| Supprimer | #DC2626 (danger) |

## 6. APIS & ENDPOINTS

```
GET /ipLoadbalancing/{serviceName}/http/frontend
GET /ipLoadbalancing/{serviceName}/tcp/frontend
GET /ipLoadbalancing/{serviceName}/http/frontend/{frontendId}
POST /ipLoadbalancing/{serviceName}/http/frontend
PUT /ipLoadbalancing/{serviceName}/http/frontend/{frontendId}
DELETE /ipLoadbalancing/{serviceName}/http/frontend/{frontendId}
```

## 7. TRADUCTIONS (i18n)

```json
{
  "frontends": "Frontends",
  "manage_frontends_description": "Gérez les points d'entrée de votre Load Balancer",
  "add_frontend": "Ajouter un frontend",
  "frontend": "Frontend",
  "protocol": "Protocole",
  "datacenter": "Datacenter",
  "default_farm": "Ferme par défaut",
  "active": "Actif",
  "view_preview": "Voir l'aperçu",
  "modify": "Modifier",
  "delete": "Supprimer"
}
```
