# Cahier des Charges : network/load-balancer - SSL Certificates

## 1. VUE D'ENSEMBLE

**Type:** Page listing avec datagrid
**Fonction:** Gérer les certificats SSL pour les frontends HTTPS
**Contexte:** Configuration load balancer

## 2. LAYOUT

- Page: 1200x500px
- Background: #F9FAFB
- Header OVHcloud: 56px, #000E9C

## 3. HEADER PAGE

- Titre: "Certificats SSL" (22px, font-weight: 600)
- Sous-titre: "Gérez les certificats SSL pour vos frontends HTTPS" (text-muted, 13px)

## 4. ACTION BAR

| Bouton | Style |
|--------|-------|
| + Ajouter un certificat | Secondary (text-link) |
| Commander un certificat | Secondary (text-link) |

## 5. DATAGRID

### Conteneur
- Width: 1120px
- Background: #FFFFFF
- Border-radius: 8px
- Border: #E5E7EB

### Colonnes
| Colonne | Largeur | Type |
|---------|---------|------|
| Certificat | 320px | Link (text-link, nom + ID) |
| Sujet | 350px | Texte |
| Type | 350px | Texte |
| Actions | auto | Menu ⋮ |

### Types certificat
- Custom
- Let's Encrypt
- OVH DV
- OVH EV

### Pagination
- "Affichage X-Y sur Z certificats"

## 6. APIS & ENDPOINTS

```
GET /ipLoadbalancing/{serviceName}/ssl
POST /ipLoadbalancing/{serviceName}/ssl
DELETE /ipLoadbalancing/{serviceName}/ssl/{sslId}
POST /order/cart/{cartId}/ipLoadbalancing/{options}/ssl
```

## 7. TRADUCTIONS (i18n)

```json
{
  "ssl_certificates": "Certificats SSL",
  "ssl_certificates_description": "Gérez les certificats SSL pour vos frontends HTTPS",
  "add_certificate": "Ajouter un certificat",
  "order_certificate": "Commander un certificat",
  "certificate": "Certificat",
  "subject": "Sujet",
  "type": "Type",
  "custom": "Custom",
  "lets_encrypt": "Let's Encrypt",
  "display_count": "Affichage {start}-{end} sur {total} certificats"
}
```
