# Cahier des Charges : network/load-balancer - Server Farms

## 1. VUE D'ENSEMBLE

**Type:** Page listing avec accordéons
**Fonction:** Gérer les fermes de serveurs et serveurs backend
**Contexte:** Configuration load balancer

## 2. LAYOUT

- Page: 1200x650px
- Background: #F9FAFB
- Header OVHcloud: 56px, #000E9C

## 3. HEADER PAGE

- Titre: "Server Farms" (22px, font-weight: 600)
- Sous-titre: "Gérez vos fermes de serveurs et les serveurs backend" (text-muted, 13px)

## 4. ACTION BAR

- Bouton: "+ Ajouter une farm" (secondary, text-link)

## 5. ACCORDÉON FERME

### Header accordéon (fermé)
| Élément | Style |
|---------|-------|
| Background | #F3F4F6 |
| Height | 50px |
| Chevron | ▶ (fermé) / ▼ (ouvert) |
| Label | "farm-name (PROTOCOL - Port XXX)" (font-weight: 600) |
| Menu | ⋮ (ActionMenu) |

### Contenu accordéon (ouvert)
- Background: #FFFFFF
- Border-radius: 0 0 8px 8px
- Bouton: "+ Ajouter un serveur" (secondary, text-link)

### Tableau serveurs
| Colonne | Largeur | Type |
|---------|---------|------|
| Serveur | 220px | Texte |
| IPv4 | 200px | Texte |
| Actif | 150px | Toggle switch |
| Statut | 450px | Badge (UP/DOWN) |
| Actions | auto | Menu ⋮ |

### Toggle switch
- Active: #0050D7 avec cercle blanc à droite
- Inactive: #E5E7EB avec cercle blanc à gauche

### Badge statut
| État | Background | Text |
|------|------------|------|
| UP | #DCFCE7 | #16A34A |
| DOWN | #FEE2E2 | #DC2626 |

## 6. APIS & ENDPOINTS

```
GET /ipLoadbalancing/{serviceName}/http/farm
GET /ipLoadbalancing/{serviceName}/tcp/farm
GET /ipLoadbalancing/{serviceName}/http/farm/{farmId}/server
POST /ipLoadbalancing/{serviceName}/http/farm
POST /ipLoadbalancing/{serviceName}/http/farm/{farmId}/server
DELETE /ipLoadbalancing/{serviceName}/http/farm/{farmId}
```

## 7. TRADUCTIONS (i18n)

```json
{
  "server_farms": "Server Farms",
  "server_farms_description": "Gérez vos fermes de serveurs et les serveurs backend",
  "add_farm": "Ajouter une farm",
  "add_server": "Ajouter un serveur",
  "server": "Serveur",
  "ipv4": "IPv4",
  "active": "Actif",
  "status": "Statut",
  "up": "UP",
  "down": "DOWN"
}
```
