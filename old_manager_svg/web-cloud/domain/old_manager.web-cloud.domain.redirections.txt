# Cahier des Charges : web-cloud/domain - Redirections

## 1. VUE D'ENSEMBLE

**Type:** Page placeholder
**Fonction:** Page des redirections web (en construction)
**Contexte:** Onglet Redirections d'un domaine

## 2. NAVIGATION

### NAV1
- Menu: Web Cloud (actif)

### NAV2
- Menu: Domaines et DNS (actif)

## 3. CONTENU ACTUEL

### État: En construction
- Message centré: "Redirections"
- Sous-message: "Page en cours de construction"

## 4. CONTENU ATTENDU (Spécification cible)

Cette page devrait afficher la liste des redirections web configurées.

### Structure attendue
- Bouton: "+ Ajouter une redirection"
- Tableau des redirections existantes

### Colonnes attendues

| Colonne | Description |
|---------|-------------|
| Source | Sous-domaine source |
| Type | 301, 302, ou iFrame |
| Destination | URL cible |
| Actions | Modifier, Supprimer |

### Actions
- Ajouter une redirection (ouvre modal-redirection-add)
- Modifier une redirection
- Supprimer une redirection

## 5. APIS & ENDPOINTS

```
GET /domain/zone/{zoneName}/redirection
→ Liste des redirections

GET /domain/zone/{zoneName}/redirection/{id}
→ Détails d'une redirection

POST /domain/zone/{zoneName}/redirection
Body: {
  subDomain: "www",
  type: "visible" | "visiblePermanent" | "invisible",
  target: "https://example.com"
}
→ Crée une redirection

PUT /domain/zone/{zoneName}/redirection/{id}
→ Modifie une redirection

DELETE /domain/zone/{zoneName}/redirection/{id}
→ Supprime une redirection
```

## 6. TRADUCTIONS (i18n)

| Clé | FR |
|-----|-----|
| page_title | Redirections |
| under_construction | Page en cours de construction |
| btn_add | + Ajouter une redirection |
| col_source | Source |
| col_type | Type |
| col_destination | Destination |
| col_actions | Actions |
