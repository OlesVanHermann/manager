# old_manager.telecom.sms.senders.txt
# Cahier des charges - Expéditeurs SMS
# Généré depuis : old_manager_web-cloud_telecom_sms.tar

---

## 1. RÉSUMÉ EXÉCUTIF

### Périmètre
Page de gestion des expéditeurs (senders) d'un compte SMS.

### Objectif fonctionnel
Permettre la création, validation et gestion des noms d'expéditeurs pour les SMS.

### Note
Le code source n'est pas fourni. Spécification basée sur les SVG existants.

---

## 2. ARCHITECTURE

### Pages liées (SVG existants)
- old_manager.telecom.sms.senders.svg - Liste des expéditeurs
- old_manager.telecom.sms.sender-add.svg - Ajout expéditeur

---

## 3. PAGES & NAVIGATION

### Route probable
| Propriété | Valeur |
|-----------|--------|
| State | sms.service.senders |
| URL | /sms/:serviceName/senders |

---

## 4. COMPOSANTS UI

### Liste expéditeurs (Datagrid)
| Colonne | Description |
|---------|-------------|
| Nom | Nom de l'expéditeur |
| Statut | Validé/En attente/Refusé |
| Type | Numéro/Alphanumérique |
| Actions | Modifier/Supprimer |

---

## 5. APIs & DONNÉES

### Endpoints probables
| Méthode | Endpoint | Description |
|---------|----------|-------------|
| GET | /sms/{serviceName}/senders | Liste expéditeurs |
| POST | /sms/{serviceName}/senders | Création |
| PUT | /sms/{serviceName}/senders/{sender} | Modification |
| DELETE | /sms/{serviceName}/senders/{sender} | Suppression |

### Modèle de données probable
```typescript
interface SmsSender {
  sender: string;
  status: 'pending' | 'valid' | 'refused';
  type: 'numeric' | 'alpha';
  description: string;
}
```

---

## 6. TRADUCTIONS

### Clés i18n probables
| Clé | FR (estimé) |
|-----|-----|
| sms_senders_title | "Expéditeurs" |
| sms_senders_add | "Ajouter un expéditeur" |
| sms_senders_status_pending | "En attente de validation" |
| sms_senders_status_valid | "Validé" |
| sms_senders_status_refused | "Refusé" |

---

## 7. RÈGLES MÉTIER

### Validation expéditeur
| Type | Règles |
|------|--------|
| Alphanumérique | 3-11 caractères, lettres et chiffres |
| Numérique | Format numéro valide |

### Note importante
CODE SOURCE NON FOURNI - Spécification basée sur les SVG existants.
