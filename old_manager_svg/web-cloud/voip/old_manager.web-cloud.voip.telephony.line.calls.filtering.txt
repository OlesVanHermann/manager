# Cahier des Charges : telecom/telephony - Filtrage des appels

## 1. VUE D'ENSEMBLE
- **Type** : Page de configuration
- **URL** : /telephony/{billingAccount}/line/{serviceName}/calls/filtering
- **Objectif** : Configurer les règles de filtrage pour les appels entrants et sortants

## 2. DIMENSIONS
| Propriété | Valeur |
|-----------|--------|
| Largeur | 1200px |
| Hauteur | 800px |

## 3. COMPOSANTS UI
- **OdsHeader** : En-tête OVHcloud
- **OdsBreadcrumb** : Télécom / Téléphonie / Ligne / Appels
- **OdsLink** : Retour aux appels
- **OdsCard** : Configuration + Ajout filtre + Liste filtres
- **OdsSelect** : Type de filtrage
- **OdsCheckbox** : Options globales
- **OdsInput** : Numéro/préfixe
- **OdsDatagrid** : Tableau des filtres
- **OdsBadge** : Type de filtre
- **OdsButton** : Ajouter, Supprimer

## 4. HEADER PAGE
| Élément | Description |
|---------|-------------|
| Lien retour | "← Retour aux appels" |
| Titre | "Filtrage des appels" |

## 5. CARD ACTIVER LE FILTRAGE
| Propriété | Valeur |
|-----------|--------|
| Titre | "Activer le filtrage" |
| Description | "Configurez les règles de filtrage pour les appels entrants et sortants." |

### Configuration par Direction
| Champ | Type | Options |
|-------|------|---------|
| Appels entrants | Select | Aucun filtrage, Liste blanche, Liste noire |
| Appels sortants | Select | Aucun filtrage, Liste blanche, Liste noire |

### Options Globales
| Checkbox | Label |
|----------|-------|
| Non coché | "Rejeter les appels anonymes" |
| Non coché | "Ne pas déranger (rejeter tous les appels)" |

## 6. CARD AJOUTER UN FILTRE
| Propriété | Valeur |
|-----------|--------|
| Titre | "Ajouter un filtre" |

### Champs
| Champ | Type | Placeholder |
|-------|------|-------------|
| Numéro ou préfixe | Input | "Numéro ou préfixe" |
| Direction | Select | Entrant, Sortant |

### Bouton
| Bouton | Style |
|--------|-------|
| Ajouter | Primary |

## 7. CARD FILTRES CONFIGURÉS
| Propriété | Valeur |
|-----------|--------|
| Titre | "Filtres configurés" |

### Colonnes Tableau
| Colonne | Type |
|---------|------|
| Numéro/Préfixe | Text |
| Direction | Text |
| Type | Badge |
| Actions | 🗑 Supprimer |

### Types de Filtre
| Type | Style |
|------|-------|
| Liste blanche | Badge vert |
| Liste noire | Badge rouge |

### Exemple
| Préfixe | Direction | Type |
|---------|-----------|------|
| 0899* | Sortant | Liste noire |

## 8. APIS & ENDPOINTS
- GET /telephony/{billingAccount}/line/{serviceName}/options/callFiltering - Configuration
- PUT /telephony/{billingAccount}/line/{serviceName}/options/callFiltering - Modifier
- GET /telephony/{billingAccount}/line/{serviceName}/callFiltering/rules - Liste règles
- POST /telephony/{billingAccount}/line/{serviceName}/callFiltering/rules - Ajouter règle
- DELETE /telephony/{billingAccount}/line/{serviceName}/callFiltering/rules/{ruleId} - Supprimer

