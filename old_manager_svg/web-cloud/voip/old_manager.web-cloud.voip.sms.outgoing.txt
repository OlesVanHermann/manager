# old_manager.telecom.sms.outgoing.txt
# Cahier des charges - SMS Envoyés
# Généré depuis : old_manager_web-cloud_telecom_sms.tar

---

## 1. RÉSUMÉ EXÉCUTIF

### Périmètre
Page d'historique des SMS envoyés d'un compte SMS.

### Objectif fonctionnel
Permettre la consultation de l'historique des SMS sortants avec statut de livraison.

### Note
Le code source n'est pas fourni. Spécification basée sur les SVG existants.

---

## 2. ARCHITECTURE

### Pages liées (SVG existants)
- old_manager.telecom.sms.outgoing.svg - Historique SMS envoyés

---

## 3. PAGES & NAVIGATION

### Route probable
| Propriété | Valeur |
|-----------|--------|
| State | sms.service.outgoing |
| URL | /sms/:serviceName/outgoing |

---

## 4. COMPOSANTS UI

### Filtres
| Filtre | Type | Description |
|--------|------|-------------|
| Période | Date range | Du / Au |
| Destinataire | Input | Recherche par numéro |
| Expéditeur | Select | Filtre par sender |
| Statut | Select | Filtre par statut |

### Historique (Datagrid)
| Colonne | Description |
|---------|-------------|
| Date | Date/heure envoi |
| Destinataire | Numéro destination |
| Expéditeur | Sender utilisé |
| Message | Contenu (tronqué) |
| Crédits | Nombre de crédits utilisés |
| Statut | Livré/Échec/En cours |

---

## 5. APIs & DONNÉES

### Endpoints probables
| Méthode | Endpoint | Description |
|---------|----------|-------------|
| GET | /sms/{serviceName}/outgoing | Liste SMS envoyés |
| GET | /sms/{serviceName}/outgoing/{id} | Détail SMS |

### Modèle de données probable
```typescript
interface SmsOutgoing {
  id: number;
  creationDatetime: string;
  credits: number;
  deliveryReceipt: number;
  message: string;
  receiver: string;
  sender: string;
}
```

---

## 6. TRADUCTIONS

### Clés i18n probables
| Clé | FR (estimé) |
|-----|-----|
| sms_outgoing_title | "SMS envoyés" |
| sms_outgoing_status_sent | "Envoyé" |
| sms_outgoing_status_delivered | "Livré" |
| sms_outgoing_status_failed | "Échec" |

---

## 7. RÈGLES MÉTIER

### Statuts de livraison
| Code | Statut | Description |
|------|--------|-------------|
| 0 | Envoyé | SMS transmis à l'opérateur |
| 1 | Livré | SMS reçu par destinataire |
| 2 | Échec | Échec de livraison |

### Note importante
CODE SOURCE NON FOURNI - Spécification basée sur les SVG existants.
