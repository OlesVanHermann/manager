# Cahier des Charges : web-cloud/hosting - Configuration Git

## 1. VUE D'ENSEMBLE
- **Page** : Configuration du déploiement Git
- **Type** : Page de configuration (pas une modal)
- **Objectif** : Associer un dépôt Git à l'hébergement

## 2. LAYOUT & STRUCTURE
- Lien retour "← Retour"
- Titre : "Configurer votre Git pour votre répertoire /www"
- Sous-titre : "Votre configuration s'appliquera sur l'ensemble des domaines."
- Bandeau info bleu
- Formulaire de configuration
- Sections SSH et Webhook

## 3. COMPOSANTS UI
- **OdsLink** : Retour
- **OdsMessage** : Bandeau info bleu
- **OdsInput** : Dépôt, Branche, Clé SSH, URL webhook
- **OdsButton** : Copier (icône 📋), Appliquer la configuration

## 4. BANDEAU INFO
"ℹ Configurer le déploiement de vos fichiers sur votre dépôt Git"
- HTTPS pour un dépôt public (https://github.com/username/)
- SSH pour un dépôt privé (git@github.com:username/)
- **Le répertoire d'installation doit être vide**

## 5. FORMULAIRE
### Configurer votre déploiement
| Champ | Valeur exemple |
|-------|----------------|
| Dépôt | git@github.com:mycompany/myproject.git |
| Branche | main |

### Clé SSH
- Input readonly avec clé tronquée
- Bouton copier (📋)

### Url de webhook
- Input readonly avec URL complète
- Bouton copier (📋)
- Format : https://hosting123.cluster456.hosting.ovh.net/webhook/git

## 6. BOUTON ACTION
- "Appliquer la configuration" (primaire)

## 7. APIS & ENDPOINTS
- GET /hosting/web/{serviceName}/runtime/attachedDomain/{domain}/git : Config Git actuelle
- PUT /hosting/web/{serviceName}/runtime/attachedDomain/{domain}/git : Configurer Git
- GET /hosting/web/{serviceName}/sshKey : Récupérer clé SSH
