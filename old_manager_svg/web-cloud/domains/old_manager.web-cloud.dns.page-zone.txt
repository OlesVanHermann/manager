# Cahier des Charges : web-cloud/dns - Page Zone

## 1. VUE D'ENSEMBLE
- **Type** : Page détail zone DNS
- **Contexte** : Web Cloud DNS Management
- **Objectif** : Gérer les enregistrements d'une zone DNS

## 2. DIMENSIONS
| Propriété | Valeur |
|-----------|--------|
| Page largeur | 1100px |
| Page hauteur | 600px |
| Background | #F9FAFB |

## 3. COMPOSANTS UI
- **OdsText** : Titres et labels
- **OdsLink** : Retour et actions
- **OdsBadge** : Statut et DNSSEC
- **OdsButton** : Import, Export, Ajouter
- **OdsCard** : Conteneur principal
- **OdsInput** : Recherche
- **OdsSelect** : Filtre type
- **OdsDatagrid** : Liste enregistrements
- **OdsPagination** : Navigation

## 4. HEADER
| Propriété | Valeur |
|-----------|--------|
| Retour | "← Retour aux zones DNS" |
| Titre | "Zone DNS : {zoneName}" |
| Badge statut | Actif (vert) |
| Badge DNSSEC | "DNSSEC ✓" (vert) |
| Boutons | Exporter, Importer, + Ajouter entrée |

## 5. FILTRES
| Élément | Type |
|---------|------|
| Recherche | Input "🔍 Rechercher..." |
| Type | Select "Type ▾" |

## 6. DATAGRID ENREGISTREMENTS
| Colonne | Description |
|---------|-------------|
| Type | Badge coloré (A, AAAA, CNAME, MX, TXT...) |
| Sous-domaine | "@", "www", "api", "_dmarc"... |
| Cible / Valeur | IP, hostname, valeur TXT (monospace) |
| TTL | Durée en secondes |
| Actions | Modifier (bleu), Supprimer (rouge) |

## 7. BADGES TYPE DNS
| Type | Fond | Texte |
|------|------|-------|
| A | #16A34A | white |
| AAAA | #7C3AED | white |
| CNAME | #0050D7 | white |
| MX | #F59E0B | white |
| TXT | #DC2626 | white |

## 8. PAGINATION
| Propriété | Valeur |
|-----------|--------|
| Format | "XX enregistrements • Page X sur Y" |
| Boutons | ‹ Numéros › |

## 9. APIS & ENDPOINTS
- GET /domain/zone/{zoneName} - Détail zone
- GET /domain/zone/{zoneName}/record - Liste enregistrements
- POST /domain/zone/{zoneName}/record - Créer enregistrement
- PUT /domain/zone/{zoneName}/record/{id} - Modifier
- DELETE /domain/zone/{zoneName}/record/{id} - Supprimer
- POST /domain/zone/{zoneName}/export - Export zone
- POST /domain/zone/{zoneName}/import - Import zone


