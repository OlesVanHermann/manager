# Cahier des Charges : public-cloud/ai-dashboard - Modal Configurer dépot Git privé

## 1. VUE D'ENSEMBLE
- **Modal** : Configuration d'un dépôt Git privé
- **Type** : Modal formulaire
- **Objectif** : Ajouter un dépôt Git privé avec authentification

## 2. STRUCTURE MODAL
- Header avec titre et bouton fermer (×)
- Champs: Alias, Endpoint, Région
- Type authentification (radio: Basic / SSH)
- Champs auth (Username, Password)
- Texte explicatif
- Règles de validation
- Boutons Annuler / Ajouter

## 3. COMPOSANTS UI
- **OdsModal** : Conteneur (700x770)
- **OdsInput** : Alias, Endpoint, Username, Password
- **OdsSelect** : Région
- **OdsRadio** : Type auth (Basic, SSH)
- **OdsCard** : Texte explicatif (fond gris)
- **OdsButton** : Annuler, Ajouter

## 4. TITRE
"Configurer un dépot Git privé"

## 5. CHAMPS
### Alias *
- Type: Input
- Exemple: "my-git-repo"

### Endpoint *
- Type: Input
- Placeholder: "https://github.com/git-project"

### Région *
- Type: Select
- Valeur: "GRA (Gravelines)"

### Type d'authentification *
| Option | État |
|--------|------|
| Authentification basic | Sélectionné |
| Clé SSH | Non sélectionné |

### Nom d'utilisateur * (si Basic Auth)
- Type: Input
- Exemple: "git-username"

### Mot de passe * (si Basic Auth)
- Type: Input password
- Valeur: "••••••••••••"
- Icône: 👁

## 6. TEXTE EXPLICATIF
Fond gris (#F3F4F6) :
"Les dépots Git privés vous permettent de configurer tous les éléments"
"nécessaires pour que vos produits IA puissent communiquer avec vos"
"projets GIT. Les dépots Git privés sont spécifiques aux produits AI."

## 7. RÈGLES VALIDATION
- "Doit uniquement contenir des nombres, lettres, underscores ou tirets"
- "Ne doit pas commencer par un tiret"

## 8. BOUTONS
| Bouton | Style | Action |
|--------|-------|--------|
| Annuler | Outline | Ferme la modal |
| Ajouter | Primaire bleu | Enregistre le dépôt |

## 9. APIS & ENDPOINTS
- POST /cloud/project/{projectId}/ai/git
  - alias: string
  - endpoint: string
  - region: string
  - authType: "basic" | "ssh"
  - username: string (si basic)
  - password: string (si basic)
  - sshPrivateKey: string (si ssh)

