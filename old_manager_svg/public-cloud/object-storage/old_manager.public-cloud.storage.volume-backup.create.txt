# Cahier des Charges : public-cloud/storage/volume-backup/create

## 1. VUE D'ENSEMBLE

| Champ | Valeur |
|-------|--------|
| **Fichier SVG** | old_manager.public-cloud.storage.volume-backup.create.svg |
| **NAV1** | public-cloud |
| **NAV2** | storage |
| **Type** | Page formulaire |
| **Description** | Formulaire de création d'un backup de volume Block Storage |

---

## 2. LAYOUT

| Zone | Valeur |
|------|--------|
| Type | Page pleine largeur (sans LEFT PANEL) |
| Breadcrumb | mon-projet / Stockage / Volume Backup / Créer un backup |

---

## 3. HEADER

| Titre | Créer un backup |
| Font-size | 22px |
| Font-weight | 600 |
| Bouton guides | 📖 Guides (100×32px, outline) |
| Description | Créez une sauvegarde de votre volume Block Storage. |

---

## 4. ÉTAPE 1 : SÉLECTION DU VOLUME

| Titre | 1. Sélectionner un volume |
| Font-size | 16px |
| Font-weight | 600 |
| Champ | Select (400×40px) |
| Exemple | data-volume-prod-01 (100 GiB) |

---

## 5. ÉTAPE 2 : TYPE DE BACKUP

| Titre | 2. Type de backup |
| Lien | En savoir plus sur les types de backup ↗ |

### 5.1 Cartes de sélection

| Carte | Dimensions | Description |
|-------|------------|-------------|
| Volume Snapshot | 300×150px | Sauvegarde rapide basée sur la copie instantanée |
| Volume Backup | 300×150px | Sauvegarde complète indépendante du volume source |

### 5.2 Prix

| Type | Prix |
|------|------|
| Snapshot | 0,0100€ HT/GiB/mois |
| Backup | 0,0050€ HT/GiB/mois |

---

## 6. ÉTAPE 3 : NOM DU BACKUP

| Titre | 3. Nom du backup |
| Font-size | 16px |
| Font-weight | 600 |
| Champ | Input text (400×40px) |
| Exemple | backup-data-volume-prod-01-20260102 |

---

## 7. FOOTER

| Bouton | Style | Dimensions |
|--------|-------|------------|
| Créer le backup | Primary | 180×48px |

---

## 8. APIS & ENDPOINTS

### 8.1 Lister les volumes

```
GET /cloud/project/{projectId}/volume
```

### 8.2 Créer un snapshot

```
POST /cloud/project/{projectId}/volume/{volumeId}/snapshot
```

Body:
```json
{
  "name": "backup-data-volume-prod-01-20260102"
}
```

### 8.3 Créer un backup complet

```
POST /cloud/project/{projectId}/volume/{volumeId}/backup
```

Body:
```json
{
  "name": "backup-data-volume-prod-01-20260102"
}
```

---

## 9. TRADUCTIONS (i18n)

| Clé | Valeur FR |
|-----|-----------|
| `pci_storage_backup_create_title` | Créer un backup |
| `pci_storage_backup_create_desc` | Créez une sauvegarde de votre volume Block Storage. |
| `pci_storage_backup_create_step1` | Sélectionner un volume |
| `pci_storage_backup_create_volume` | Volume |
| `pci_storage_backup_create_step2` | Type de backup |
| `pci_storage_backup_create_types_link` | En savoir plus sur les types de backup |
| `pci_storage_backup_create_snapshot` | Volume Snapshot |
| `pci_storage_backup_create_snapshot_desc` | Sauvegarde rapide basée sur la copie instantanée |
| `pci_storage_backup_create_backup` | Volume Backup |
| `pci_storage_backup_create_backup_desc` | Sauvegarde complète indépendante du volume source |
| `pci_storage_backup_create_step3` | Nom du backup |
| `pci_storage_backup_create_name` | Nom |
| `pci_storage_backup_create_submit` | Créer le backup |
