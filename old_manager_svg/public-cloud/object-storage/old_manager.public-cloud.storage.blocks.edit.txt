# Cahier des Charges : public-cloud/storage/blocks/edit

## 1. VUE D'ENSEMBLE

| Champ | Valeur |
|-------|--------|
| **Fichier SVG** | old_manager.public-cloud.storage.blocks.edit.svg |
| **NAV1** | public-cloud |
| **NAV2** | storage |
| **Type** | Page formulaire |
| **Description** | Formulaire de modification d'un volume Block Storage |

---

## 2. LAYOUT

| Zone | Valeur |
|------|--------|
| Type | Page pleine largeur (sans LEFT PANEL) |
| Breadcrumb | mon-projet / Stockage / Block Storage / Modifier le volume |

---

## 3. HEADER

| Titre | Modifier le volume |
| Font-size | 22px |
| Font-weight | 600 |

---

## 4. FORMULAIRE

### 4.1 Dimensions

| Élément | Dimensions |
|---------|------------|
| Card formulaire | 800×520px |
| Padding | 32px |

### 4.2 Champs du formulaire

| Champ | Type | Largeur | Editable |
|-------|------|---------|----------|
| Nom du volume | Input text | 400px | Oui |
| Taille du volume | Slider + Input | 400px + 100px | Oui (augmentation uniquement) |
| Type de volume | Text disabled | 200px | Non |
| Région | Text disabled | 200px | Non |

### 4.3 Slider taille

| Propriété | Valeur |
|-----------|--------|
| Largeur | 400px |
| Hauteur | 8px |
| Background track | #E5E7EB |
| Background fill | #0050D7 |
| Thumb size | 24px (rayon 12px) |
| Thumb color | #0050D7 |

### 4.4 Valeurs exemple

| Champ | Valeur |
|-------|--------|
| Nom | data-volume-prod-01 |
| Taille | 200 GiB |
| Type | High Speed (non modifiable) |
| Région | GRA11 - Gravelines (non modifiable) |

---

## 5. ESTIMATION PRIX

| Dimensions | 736×50px |
| Background | #DBEAFE |
| Border radius | 6px |
| Texte | 💰 Prix estimé : 0,0200€ HT/heure (~14,40€ HT/mois) pour 200 GiB High Speed |
| Font-size | 12px |
| Couleur texte | #1E40AF |

---

## 6. FOOTER FORMULAIRE

| Bouton | Style | Dimensions |
|--------|-------|------------|
| Annuler | Outline | 100×40px |
| Modifier | Primary | 120×40px |

---

## 7. APIS & ENDPOINTS

### 7.1 Obtenir les détails du volume

```
GET /cloud/project/{projectId}/volume/{volumeId}
```

Response:
```json
{
  "id": "vol-abc123-def456",
  "name": "data-volume-prod-01",
  "region": "GRA11",
  "size": 100,
  "type": "high-speed",
  "status": "in-use",
  "attachedTo": ["instance-id"]
}
```

### 7.2 Modifier le volume

```
PUT /cloud/project/{projectId}/volume/{volumeId}
```

Body:
```json
{
  "name": "data-volume-prod-01",
  "size": 200
}
```

### 7.3 Obtenir les tarifs

```
GET /cloud/project/{projectId}/capabilities/volume
```

---

## 8. TRADUCTIONS (i18n)

| Clé | Valeur FR |
|-----|-----------|
| `pci_storage_blocks_edit_title` | Modifier le volume |
| `pci_storage_blocks_edit_name` | Nom du volume |
| `pci_storage_blocks_edit_size` | Taille du volume |
| `pci_storage_blocks_edit_size_hint` | La taille peut uniquement être augmentée |
| `pci_storage_blocks_edit_type` | Type de volume |
| `pci_storage_blocks_edit_type_hint` | Le type ne peut pas être modifié |
| `pci_storage_blocks_edit_region` | Région |
| `pci_storage_blocks_edit_region_hint` | La région ne peut pas être modifiée |
| `pci_storage_blocks_edit_price` | Prix estimé |
| `pci_storage_blocks_edit_cancel` | Annuler |
| `pci_storage_blocks_edit_submit` | Modifier |
