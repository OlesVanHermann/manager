# Cahier des Charges : telecom/telephony - Présentation de numéros externes (Trunk)

## 1. VUE D'ENSEMBLE
- **Type** : Page de gestion
- **URL** : /telephony/{billingAccount}/line/{serviceName}/calls/externalNumber
- **Objectif** : Configurer la présentation de numéros externes lors des appels sortants

## 2. DIMENSIONS
| Propriété | Valeur |
|-----------|--------|
| Largeur | 1200px |
| Hauteur | 700px |

## 3. COMPOSANTS UI
- **OdsHeader** : En-tête OVHcloud
- **OdsBreadcrumb** : Télécom / Téléphonie / Ligne / Appels
- **OdsLink** : Retour aux appels
- **OdsCard** : Liste numéros + Ajout numéro
- **OdsDatagrid** : Tableau des numéros
- **OdsBadge** : Statut (Validé, En attente, Refusé)
- **OdsMessage** : Info processus validation + Succès
- **OdsInput** : Saisie numéro
- **OdsCheckbox** : Validation VIP
- **OdsButton** : Ajouter, Supprimer

## 4. HEADER PAGE
| Élément | Description |
|---------|-------------|
| Lien retour | "← Retour aux appels" |
| Titre | "Présentation de numéros externes" |

## 5. LAYOUT 2 COLONNES
| Colonne | Contenu |
|---------|---------|
| Gauche (550px) | Liste des numéros configurés |
| Droite (550px) | Formulaire d'ajout |

## 6. CARD NUMÉROS CONFIGURÉS
| Propriété | Valeur |
|-----------|--------|
| Titre | "Numéros configurés" |

### Colonnes Tableau
| Colonne | Type |
|---------|------|
| Numéro | Text |
| Statut | Badge |
| Actions | 🗑 Supprimer |

### Statuts
| Statut | Style |
|--------|-------|
| Validé | Badge vert |
| En attente | Badge jaune |
| Refusé | Badge rouge |

### Note
| Texte |
|-------|
| "Vous pouvez présenter jusqu'à 2 numéros externes (lignes simultanées: 2)." |

## 7. CARD AJOUT NUMÉRO
| Propriété | Valeur |
|-----------|--------|
| Titre | "Ajouter un numéro externe" |

### Message Info
| Type | Contenu |
|------|---------|
| Info (bleu) | "Un numéro externe vous permet de présenter un numéro qui ne vous appartient pas lors de vos appels sortants. La validation se fait par un appel entrant sur votre ligne. Vous devrez saisir le code de validation qui vous sera communiqué." |

### Champs
| Champ | Type | Placeholder |
|-------|------|-------------|
| Numéro à ajouter | Input tel | "+33 1 23 45 67 89" |
| Validation automatique (VIP) | Checkbox | Option spéciale |

### Boutons
| Bouton | Style | Action |
|--------|-------|--------|
| Annuler | Secondary | Réinitialise |
| Ajouter | Primary | Soumet la demande |

### Message Succès
| Type | Contenu |
|------|---------|
| Success (vert) | "✓ Demande envoyée avec succès ! Code de validation: 1234. Appelez depuis ce numéro et saisissez ce code." |

## 8. APIS & ENDPOINTS
- GET /telephony/{billingAccount}/line/{serviceName}/options/externalNumber - Liste numéros externes
- POST /telephony/{billingAccount}/line/{serviceName}/options/externalNumber - Ajouter
- DELETE /telephony/{billingAccount}/line/{serviceName}/options/externalNumber/{number} - Supprimer
- POST /telephony/{billingAccount}/line/{serviceName}/options/externalNumber/{number}/validate - Valider

